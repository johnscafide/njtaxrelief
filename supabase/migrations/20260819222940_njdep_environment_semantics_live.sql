insert into public.data_center_provider_coverage
  (marker_id, scopes, provider_key, value_status, source_keys, last_verified_at, notes, provider_kind, source_fields, calculation_key, freshness_seconds, cache_policy, bulk_capable)
values
  (
    'watchdog.njplus.environmental_record_resolution',
    array['property']::text[],
    'watchdog-derived',
    'live',
    array['njdep-csrr-gis']::text[],
    now(),
    'Checked-source completeness across eight exact parcel-attributed NJDEP CSRR facts. A source_checked_no_value result counts as successful resolution coverage; the score is not environmental clearance.',
    'derived_governed',
    array[
      'njplus.njdep-csrr-gis.kcsl_case_status',
      'njplus.njdep-csrr-gis.kcsl_program_interest',
      'njplus.njdep-csrr-gis.kcsl_site_id',
      'njplus.njdep-csrr-gis.deed_notice_reference',
      'njplus.njdep-csrr-gis.cea_reference',
      'njplus.njdep-csrr-gis.ust_facility_reference',
      'njplus.njdep-csrr-gis.brownfield_inventory_status',
      'njplus.njdep-csrr-gis.environmental_layer_vintage'
    ]::text[],
    'watchdog-derived-v15-strict-signal-count',
    21600,
    'refresh_on_demand',
    false
  ),
  (
    'watchdog.njplus.environmental_case_activity',
    array['property']::text[],
    'watchdog-derived',
    'live',
    array['njdep-csrr-gis']::text[],
    now(),
    'Source-preserving semantic alias to exact parcel-attributed NJDEP CASE_STATUS. No numeric activity or severity interpretation is added.',
    'authoritative_reference',
    array['njplus.njdep-csrr-gis.kcsl_case_status']::text[],
    'watchdog-derived-v15-strict-signal-count',
    21600,
    'refresh_on_demand',
    false
  ),
  (
    'watchdog.njplus.brownfield_reuse_context',
    array['property']::text[],
    'watchdog-derived',
    'live',
    array['njdep-csrr-gis']::text[],
    now(),
    'Source-preserving semantic alias to exact parcel-attributed NJDEP BROWNFIELDS_DEVELOPMENT_AREA. The value is program/redevelopment context only and is not a contamination, cleanup, entitlement, or suitability determination.',
    'authoritative_reference',
    array['njplus.njdep-csrr-gis.brownfield_inventory_status']::text[],
    'watchdog-derived-v15-strict-signal-count',
    21600,
    'refresh_on_demand',
    false
  ),
  (
    'watchdog.njplus.environmental_source_currency',
    array['property']::text[],
    'watchdog-derived',
    'live',
    array['njdep-csrr-gis']::text[],
    now(),
    'Source-preserving semantic alias to the certified NJDEP environmental layer vintage. The published vintage is returned unchanged rather than converted into an arbitrary freshness score.',
    'authoritative_reference',
    array['njplus.njdep-csrr-gis.environmental_layer_vintage']::text[],
    'watchdog-derived-v15-strict-signal-count',
    21600,
    'refresh_on_demand',
    false
  )
on conflict (marker_id) do update set
  scopes=excluded.scopes,
  provider_key=excluded.provider_key,
  value_status=excluded.value_status,
  source_keys=excluded.source_keys,
  last_verified_at=excluded.last_verified_at,
  notes=excluded.notes,
  provider_kind=excluded.provider_kind,
  source_fields=excluded.source_fields,
  calculation_key=excluded.calculation_key,
  freshness_seconds=excluded.freshness_seconds,
  cache_policy=excluded.cache_policy,
  bulk_capable=excluded.bulk_capable;
