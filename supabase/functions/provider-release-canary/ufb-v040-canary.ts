import { createClient } from 'npm:@supabase/supabase-js@2.95.0';
const URL=Deno.env.get('SUPABASE_URL')!,ANON=Deno.env.get('SUPABASE_ANON_KEY')!,SERVICE=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!;
const admin=createClient(URL,SERVICE,{auth:{persistSession:false,autoRefreshToken:false}});
const SCENARIO='ufb_longitudinal_v040',PIN='0101_canary_1';
const VERSION='nj-dca-ufb-v040-longitudinal',RELEASE='nj-dca-ufb-2015-2025-v1';
const WORKBOOK_SHA='79a59be4c4ab2669d60ebb8072aab5a5775df7025e66cb95a887e1c39ed8ccaa';
const SOURCE=`NJ DCA User Friendly Budget Database · annual Summary sheets 2015-2025 · self-reported/unaudited municipal submissions · ${RELEASE}`;
const YEARS=[2015,2016,2017,2018,2019,2020,2021,2022,2023,2024,2025];
const QUALITY=YEARS.map(year=>({year,no_ufb_available:false,significant_data_missing:year===2018}));
const EXPECTED:Record<string,{value:Record<string,number>,field:string}>={
 'njplus.nj-dca-ufb-longitudinal.rut_tax_collection_pct_history':{value:{'2015':0.971,'2016':0.971,'2017':0.9715,'2018':0.9722,'2019':0.9724,'2020':0.9727,'2021':0.9728,'2022':0.9724,'2023':0.9773,'2024':0.9775,'2025':0.9825},field:'2015-2025 annual Summary sheets · Tax Collections and Delinquent Taxes · % of Tax Collections used to Calculate RUT'},
 'njplus.nj-dca-ufb-longitudinal.prior_year_realized_revenue_total_history':{value:{'2015':12324500.95,'2016':9901250.97,'2017':10611385.27,'2019':13243827.27,'2020':11464789.81,'2021':11500697.23,'2022':12827387.79,'2023':12571010.02,'2024':12839446.21,'2025':16726858.940000001},field:'2015-2025 annual Summary sheets · YEAR Total Realized Revenues (Actual) · Total'},
 'njplus.nj-dca-ufb-longitudinal.current_year_anticipated_revenue_total_history':{value:{'2015':11462505.5,'2016':9718725.129999999,'2017':10320660.51,'2018':12465000,'2019':12375000,'2020':11079925,'2021':12253600,'2022':12335391.29,'2023':12712111.52,'2024':12530261.02,'2025':14295569.48},field:'2015-2025 annual Summary sheets · YEAR Total Anticipated Revenues Current Year (Budgeted) · Total'},
 'njplus.nj-dca-ufb-longitudinal.current_year_general_budget_revenue_total_history':{value:{'2015':9849505.5,'2016':9678725.129999999,'2017':10320660.51,'2018':10575000,'2019':10755000,'2020':10898925,'2021':12049600,'2022':11970391.29,'2023':12347111.52,'2024':12280261.02,'2025':12122569.48},field:'2015-2025 annual Summary sheets · YEAR Total General Budget Revenues Current Year · Total'},
 'njplus.nj-dca-ufb-longitudinal.current_year_appropriation_total_history':{value:{'2015':11430285.51,'2016':11274815.11,'2017':11941008.219999999,'2018':12141100,'2019':12304858.59,'2020':12441881.540000001,'2021':12756504.05,'2022':13361119.06,'2023':13693379.42,'2024':14206374.350000001,'2025':14295569.479999999},field:'2015-2025 annual Summary sheets · YEAR Total Appropriations by Service Type (Current Year) · TOTAL APPROPRIATION'},
 'njplus.nj-dca-ufb-longitudinal.current_year_general_budget_appropriation_public_safety_history':{value:{'2015':2944696,'2016':2990696,'2017':3069700,'2018':3246650,'2019':3405600,'2020':3516200,'2021':3650200,'2022':3329600,'2023':3224000,'2024':3756500,'2025':3797250},field:'2015-2025 annual Summary sheets · YEAR General Budget Appropriations · Public Safety'},
 'njplus.nj-dca-ufb-longitudinal.total_personnel_cost_history':{value:{'2015':5528005,'2016':5324972.06,'2017':5601100,'2018':5740177.5,'2019':5980413,'2020':6085484,'2021':6405036,'2022':5257555,'2023':6762677.75,'2024':7042238,'2025':6341250.01},field:'2015-2025 annual Summary sheets · Total Personnel Costs · Total Personnel Cost'},
};
function json(status:number,p:any){return new Response(JSON.stringify(p),{status,headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store, private'}})}
async function hash(v:string){const d=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(v));return Array.from(new Uint8Array(d)).map(b=>b.toString(16).padStart(2,'0')).join('')}
async function cleanup(id:string){await admin.from('watchdog_test_accounts').delete().eq('user_id',id);await admin.from('account_entitlements').delete().eq('user_id',id);await admin.from('profiles').delete().eq('id',id);await admin.auth.admin.deleteUser(id)}
function sameNumber(a:any,b:number){const x=Number(a);return Number.isFinite(x)&&Math.abs(x-b)<=1e-6*Math.max(1,Math.abs(b))}
function sameHistory(a:any,b:Record<string,number>){if(!a||typeof a!=='object'||Array.isArray(a))return false;const ak=Object.keys(a).sort(),bk=Object.keys(b).sort();if(JSON.stringify(ak)!==JSON.stringify(bk))return false;return bk.every(k=>sameNumber(a[k],b[k]))}
function sameJson(a:any,b:any){return JSON.stringify(a)===JSON.stringify(b)}
export async function handleUfbV040Canary(req:Request){
 let body:any={};try{body=await req.json()}catch{return json(400,{error:'Invalid JSON'})}
 const token=String(body?.token||'').trim();if(String(body?.scenario||'')!==SCENARIO||!/^[A-Za-z0-9_-]{40,160}$/.test(token))return json(401,{error:'Invalid release canary request'});
 const now=new Date().toISOString();
 const {data:gate}=await admin.from('watchdog_test_bootstrap_tokens').update({used_at:now}).eq('token_hash',await hash(token)).is('used_at',null).gt('expires_at',now).contains('metadata',{purpose:'provider_release_canary',scenario:SCENARIO}).select('id,desired_email').maybeSingle();
 if(!gate)return json(401,{error:'Invalid or expired release canary token'});
 let userId='';
 try{
  const {data:link,error:linkError}=await admin.auth.admin.generateLink({type:'magiclink',email:String(gate.desired_email||'')});userId=String(link?.user?.id||'');const hashed=String(link?.properties?.hashed_token||'');if(linkError||!userId||!hashed)throw new Error('sandbox_link_generation_failed');
  const authClient=createClient(URL,ANON,{auth:{persistSession:false,autoRefreshToken:false,detectSessionInUrl:false}});const {data:v,error:ve}=await authClient.auth.verifyOtp({token_hash:hashed,type:'email'});const access=v?.session?.access_token||'';if(ve||!access)throw new Error('sandbox_session_verification_failed');
  const pr=await admin.from('profiles').upsert({id:userId,email:String(gate.desired_email||''),full_name:'Watchdog UFB v0.40 Canary',display_name:'Watchdog UFB v0.40 Canary',account_role:'developer',plan_tier:'standard',plan:'free',profile_complete:true,custom:{watchdog_test_account:true,no_real_spend:true,release_canary:true,scenario:SCENARIO}},{onConflict:'id'});if(pr.error)throw new Error('sandbox_profile_failed');
  const ta=await admin.from('watchdog_test_accounts').upsert({user_id:userId,label:'UFB v0.40 Canary',last_bootstrap_at:now,metadata:{no_real_spend:true,scenario:SCENARIO}},{onConflict:'user_id'});if(ta.error)throw new Error('sandbox_account_failed');
  const ids=Object.keys(EXPECTED),started=Date.now();const response=await fetch(`${URL}/functions/v1/workbench-hydrate`,{method:'POST',headers:{Authorization:`Bearer ${access}`,apikey:ANON,'Content-Type':'application/json'},body:JSON.stringify({pams_pins:[PIN],marker_ids:ids})});
  const payload=await response.json();const mismatches:string[]=[];const observed:Record<string,any>={};
  for(const id of ids){const exp=EXPECTED[id],value=payload?.markers?.[PIN]?.[id],meta=payload?.meta?.[PIN]?.[id]||{};observed[id]={value,meta};if(!sameHistory(value,exp.value))mismatches.push(`${id}:value`);if(String(meta.status||'')!=='available')mismatches.push(`${id}:status`);if(String(meta.provider_kind||'')!=='authoritative_reference')mismatches.push(`${id}:kind`);if(String(meta.source||'')!==SOURCE)mismatches.push(`${id}:source`);if(String(meta.provider_version||'')!==VERSION)mismatches.push(`${id}:version`);if(String(meta.source_release||'')!==RELEASE)mismatches.push(`${id}:release`);if(String(meta.scope||'')!=='municipality')mismatches.push(`${id}:scope`);if(String(meta.source_field||'')!==exp.field)mismatches.push(`${id}:field`);if(String(meta.workbook_sha256||'')!==WORKBOOK_SHA)mismatches.push(`${id}:workbook_sha`);if(!sameJson(meta.observation_years,YEARS))mismatches.push(`${id}:years`);if(Boolean(meta.missing_years_synthesized)!==false)mismatches.push(`${id}:synthesis`);if(String(meta.history_contract||'')!=='actual_source_observations_only')mismatches.push(`${id}:history_contract`);if(!sameJson(meta.source_quality_by_year,QUALITY))mismatches.push(`${id}:quality`) }
  if(payload?.provider_versions?.ufb_v040_longitudinal!==VERSION)mismatches.push('provider_versions');
  const ok=response.ok&&mismatches.length===0;await admin.from('watchdog_test_auth_events').insert({token_id:gate.id,user_id:userId,event_type:'provider_release_canary',metadata:{scenario:SCENARIO,status_code:response.status,duration_ms:Date.now()-started,assertion_ok:ok,mismatches}});
  return json(ok?200:502,{ok,scenario:SCENARIO,status_code:response.status,duration_ms:Date.now()-started,assertion_ok:ok,mismatches,observed,provider_versions:payload?.provider_versions||{}});
 }catch(e){return json(500,{ok:false,scenario:SCENARIO,error:String((e as Error)?.message||e)})}finally{if(userId)await cleanup(userId)}
}
