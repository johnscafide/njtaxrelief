diff --git a/property/css/lookup/07-search-corrections.css b/property/css/lookup/07-search-corrections.css
index 3e63f7d..5ebec90 100644
--- a/property/css/lookup/07-search-corrections.css
+++ b/property/css/lookup/07-search-corrections.css
@@ -48,7 +48,7 @@ body.hood-on .hd-right{position:relative;z-index:2;}
 }
 .njw-card-menu-btn:hover{background:#e8edf4!important;}
 .njw-card-menu{top:auto!important;right:12px!important;margin-top:4px!important;border-radius:10px!important;}
-.njw-card-score b.loading{color:#7d8798!important;font-weight:700!important;}
+/* Loading state retired: the score line no longer fetches a number. */
 
 /* Force the dark SEO intro to retain high contrast over inherited lookup rules. */
 .njw-seo-intro,.njw-seo-intro *{color:#fff!important;}
diff --git a/property/css/lookup/09-search-corrections-v3.css b/property/css/lookup/09-search-corrections-v3.css
index d36b83e..d1b88ae 100644
--- a/property/css/lookup/09-search-corrections-v3.css
+++ b/property/css/lookup/09-search-corrections-v3.css
@@ -44,9 +44,26 @@ body.hood-on .hd-right{overscroll-behavior-y:auto!important;scroll-padding-botto
   width:19px;height:19px;border-radius:50%;border:2px solid #10294b;background:#fff;box-shadow:0 1px 4px rgba(16,41,75,.18);pointer-events:auto;cursor:grab;
 }
 
-/* Score should look like a first-class datum while async calculation completes. */
-.njw-card-score b{min-width:40px;text-align:right!important;}
-.njw-card-score b:empty:after{content:"Calculating…";color:#7b8595;font-weight:700;}
+/* The score line is a link now, not a number. Search cards never show the
+   Watchdog Score itself: unsubscribed visitors get a Subscribe prompt, paid
+   accounts get a link through to that property's full report. */
+.njw-card-score{text-decoration:none!important;cursor:pointer;transition:color .12s ease;}
+.njw-card-score b{margin-left:auto;text-align:right!important;white-space:nowrap;}
+
+.njw-card-score-locked b{
+  color:#8a6d14;
+  background:#fdf6e2;
+  border:1px solid #f0e2b6;
+  border-radius:999px;
+  padding:2px 10px;
+  font-size:11.5px;
+  letter-spacing:.02em;
+}
+.njw-card-score-locked:hover b{background:#f8edd0;border-color:#e3d09a;}
+
+.njw-card-score-open b{color:#0e2248;}
+.njw-card-score-open b:after{content:" \2192";font-weight:700;}
+.njw-card-score-open:hover b{color:#1d4ed8;}
 
 /* The illustration is the literal end of the right-hand scroll container. */
 #pl-hood .hd-right>#njw-search-art{
diff --git a/property/developer-data.html b/property/developer-data.html
index fe022e4..e4f6965 100644
--- a/property/developer-data.html
+++ b/property/developer-data.html
@@ -12,6 +12,7 @@
   <section class="do-readiness" id="do-readiness" aria-label="SaaS production readiness"></section>
   <section class="do-factory" id="do-factory" aria-label="Watchdog Data Factory status"></section>
   <div class="do-source-alert" id="do-source-alert"></div>
+  <section class="do-source-history" id="do-source-history" aria-label="NJ publication history"></section>
   <div class="do-controls"><input id="do-search" type="search" placeholder="Search marker or source…"><select id="do-cadence"><option value="">All cadences</option><option>annual</option><option>quarterly</option><option>live</option><option>dependency</option><option>correction</option><option>review</option></select><select id="do-tier"><option value="">All plans</option><option value="standard">Standard</option><option value="pro">Pro</option><option value="pro_plus">Pro+</option></select></div>
   <div class="do-table-wrap"><table><thead><tr><th>Marker</th><th>Refresh</th><th>Source / dependency</th><th>Last refreshed</th><th>Trigger</th><th>Plan</th><th>Rule</th></tr></thead><tbody id="do-rows"></tbody></table></div>
 </section>
diff --git a/property/growth/run.js b/property/growth/run.js
index 0e5cf25..cf895a6 100644
--- a/property/growth/run.js
+++ b/property/growth/run.js
@@ -1 +1 @@
-(()=>{'use strict';const q=new URLSearchParams(location.search),profession=q.get('profession')==='mortgage'?'mortgage':'agent',play=q.get('play')||'geo-farm';const names={'geo-farm':'Strategic GeoFarm','sphere-review':'Sphere Property Review','assessment-change':'Assessment Change Conversation','recent-sale-circle':'Recent Sale Circle','long-hold':'Long-Hold Homeowners','permit-complete':'Permit & Improvement Follow-up','tax-pressure':'Tax Change Review','buyer-need':'Buyer Need Match','commercial-farm':'Small Commercial Farm','watchdog-stack':'Watchdog Signal Stack','purchase-farm':'Purchase Opportunity Farm','recent-purchase':'Recent Purchase Follow-up','assessment-payment':'Tax & Payment Context','improvement-finance':'Improvement Activity','portfolio':'Investor Portfolio Watch','refi-review':'Property Value Review','partner-farm':'Agent Partner Territory','commercial-lending':'Commercial Property Watch','change-alert':'Client Property Change Desk'},$=id=>document.getElementById(id);$('rw-title').textContent=names[play]||'Professional Growth Workflow';$('rw-kicker').textContent=(profession==='agent'?'RESIDENTIAL AGENT':'MORTGAGE PROFESSIONAL')+' · WORKFLOW BUILDER';$('rw-name').value=names[play]||'';if(play.includes('commercial'))$('rw-class').value='4A,4B,4C';if(['municipality','county','zip'].includes(q.get('scope'))) $('rw-scope').value=q.get('scope');if(q.get('value')){$('rw-value').value=q.get('value');$('rw-name').value=(names[play]||'Growth Campaign')+' · '+q.get('value')}Promise.resolve(window.njptrAccessReady).then(ctx=>{const client=window.NJPTRAccess.client(),user=ctx.user;if(!user)return;$('rw-form').onsubmit=async e=>{e.preventDefault();const btn=e.submitter,original=btn.innerHTML;btn.disabled=true;btn.innerHTML='<i class="fas fa-circle-notch fa-spin"></i> Building campaign…';const raw=$('rw-class').value,record={user_id:user.id,profession,play_key:play,name:$('rw-name').value.trim(),scope_type:$('rw-scope').value,scope_value:$('rw-value').value.trim(),criteria:{property_classes:raw?raw.split(','):[]},crm_name:$('rw-crm').value.trim()||null,status:'active'};const ins=await client.from('professional_campaigns').insert(record).select().single();if(ins.error){btn.disabled=false;btn.innerHTML=original;$('rw-result').innerHTML='<div class="gc-card"><b>Campaign could not be created</b><p>'+ins.error.message+'</p></div>';return}location.href='/property/growth/results.html?id='+encodeURIComponent(ins.data.id)}}}).catch(()=>{})})();
\ No newline at end of file
+(()=>{'use strict';const q=new URLSearchParams(location.search),profession=q.get('profession')==='mortgage'?'mortgage':'agent',play=q.get('play')||'geo-farm';const names={'geo-farm':'Strategic GeoFarm','sphere-review':'Sphere Property Review','assessment-change':'Assessment Change Conversation','recent-sale-circle':'Recent Sale Circle','long-hold':'Long-Hold Homeowners','permit-complete':'Permit & Improvement Follow-up','tax-pressure':'Tax Change Review','buyer-need':'Buyer Need Match','commercial-farm':'Small Commercial Farm','watchdog-stack':'Watchdog Signal Stack','purchase-farm':'Purchase Opportunity Farm','recent-purchase':'Recent Purchase Follow-up','assessment-payment':'Tax & Payment Context','improvement-finance':'Improvement Activity','portfolio':'Investor Portfolio Watch','refi-review':'Property Value Review','partner-farm':'Agent Partner Territory','commercial-lending':'Commercial Property Watch','change-alert':'Client Property Change Desk'},$=id=>document.getElementById(id);$('rw-title').textContent=names[play]||'Professional Growth Workflow';$('rw-kicker').textContent=(profession==='agent'?'RESIDENTIAL AGENT':'MORTGAGE PROFESSIONAL')+' · WORKFLOW BUILDER';$('rw-name').value=names[play]||'';if(play.includes('commercial'))$('rw-class').value='4A,4B,4C';if(['municipality','county','zip'].includes(q.get('scope'))) $('rw-scope').value=q.get('scope');if(q.get('value')){$('rw-value').value=q.get('value');$('rw-name').value=(names[play]||'Growth Campaign')+' · '+q.get('value')}Promise.resolve(window.njptrAccessReady).then(ctx=>{const client=window.NJPTRAccess.client(),user=ctx.user;if(!user)return;$('rw-form').onsubmit=async e=>{e.preventDefault();const btn=e.submitter,original=btn.innerHTML;btn.disabled=true;btn.innerHTML='<i class="fas fa-circle-notch fa-spin"></i> Building campaign…';const raw=$('rw-class').value,record={user_id:user.id,profession,play_key:play,name:$('rw-name').value.trim(),scope_type:$('rw-scope').value,scope_value:$('rw-value').value.trim(),criteria:{property_classes:raw?raw.split(','):[]},crm_name:$('rw-crm').value.trim()||null,status:'active'};const ins=await client.from('professional_campaigns').insert(record).select().single();if(ins.error){btn.disabled=false;btn.innerHTML=original;$('rw-result').innerHTML='<div class="gc-card"><b>Campaign could not be created</b><p>'+ins.error.message+'</p></div>';return}location.href='/property/growth/results.html?id='+encodeURIComponent(ins.data.id)}}).catch(()=>{})})();
\ No newline at end of file
diff --git a/property/index.html b/property/index.html
index 74c6c27..11a8f70 100644
--- a/property/index.html
+++ b/property/index.html
@@ -37,7 +37,7 @@
   <script async src="https://www.googletagmanager.com/gtag/js?id=G-ENP9182L0J"></script>
   <script>window.dataLayer=window.dataLayer||[];function gtag(){dataLayer.push(arguments);}gtag('js',new Date());gtag('config','G-ENP9182L0J');</script>
   <script>(function(c,l,a,r,i,t,y){c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);})(window,document,"clarity","script","wjeklv0exl");</script>
-  <script defer src="https://cdn.jsdelivr.net/npm/@emailjs/browser@4/dist/email.min.js"></script>
+  <script defer crossorigin="anonymous" src="https://cdn.jsdelivr.net/npm/@emailjs/browser@4/dist/email.min.js"></script>
   <link rel="stylesheet" href="/property/css/shared.css">
   <link rel="stylesheet" href="/property/css/lookup.css">
   <link rel="stylesheet" href="/property/css/public-mobile-nav.css">
@@ -96,7 +96,7 @@
 }
 </script>
 </head>
-<body>
+<body data-plan-auto="true">
 <!-- SITE NAV -->
 <header class="wd-nav" id="wd-nav">
   <div class="wd-nav-in">
@@ -1600,10 +1600,11 @@
     })();
 </script>
 
-<script src="https://cdnjs.cloudflare.com/ajax/libs/leaflet/1.9.4/leaflet.min.js"></script>
-<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/dist/umd/supabase.js"></script>
+<script crossorigin="anonymous" src="https://cdnjs.cloudflare.com/ajax/libs/leaflet/1.9.4/leaflet.min.js"></script>
+<script crossorigin="anonymous" src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/dist/umd/supabase.js"></script>
 <script src="https://accounts.google.com/gsi/client" async defer></script>
 <script src="/scripts.js"></script>
+<script src="/property/js/plan-context.js?v=20260812a"></script>
 <script src="/property/js/public-nav.js"></script>
 <script src="/property/js/ownership-verification.js"></script>
 <script src="/property/js/lookup.js"></script>
diff --git a/property/insights/article.html b/property/insights/article.html
index 90a537f..08ef58d 100644
--- a/property/insights/article.html
+++ b/property/insights/article.html
@@ -87,7 +87,7 @@
 </div>
 
 <footer id="main-footer"></footer>
-<script src="scripts.js"></script>
+<script src="/scripts.js"></script>
 <script>
 (function () {
   'use strict';
diff --git a/property/js/data-workbench-enhancements.js b/property/js/data-workbench-enhancements.js
index dc1dca8..1163065 100644
--- a/property/js/data-workbench-enhancements.js
+++ b/property/js/data-workbench-enhancements.js
@@ -11,7 +11,7 @@ function direct(id,r){const s=String(id||'').toLowerCase(),m=r.__markers||{},f=f
 function format(id,v){if(v==null||v==='')return null;const s=id.toLowerCase();if(/value|assessment|tax|price|reduction|overpayment/.test(s)&&!/(rate|ratio|pct|percent|year)/.test(s))return money(v);if(/rate|pct|percent/.test(s))return pct(v);if(/flood.*risk/.test(s))return String(v).replace(/^./,c=>c.toUpperCase());return String(v)}
 async function ensureFloodForRows(pins){const ids=columnIds();if(!ids.some(id=>/fema\.flood_|flood.*(zone|risk)/i.test(id)))return;const jobs=pins.map(pin=>async()=>{if(floodCache.has(pin))return;const r=cache.get(pin);if(!r||!Number.isFinite(Number(r.lat))||!Number.isFinite(Number(r.lon)))return;try{const res=await fetch(`/api/flood-zone?lat=${encodeURIComponent(r.lat)}&lon=${encodeURIComponent(r.lon)}`);if(!res.ok)return;floodCache.set(pin,await res.json())}catch(_){}});for(let i=0;i<jobs.length;i+=6)await Promise.all(jobs.slice(i,i+6).map(fn=>fn()))}
 async function hydrate(force=false){if(pending)return;const c=client(),pins=rowPins();if(!c||!pins.length)return;const signature=pins.join('|')+'::'+columnIds().join('|');if(!force&&signature===lastSignature&&pins.every(p=>cache.has(p))){paintTable();return}lastSignature=signature;pending=true;const note=$('#dw-hydrate-note');if(note){note.classList.add('dw-hydrating');note.textContent=' · checking selected fields…'}try{for(let i=0;i<pins.length;i+=500){const batch=pins.slice(i,i+500),res=await c.functions.invoke('workbench-hydrate',{body:{pams_pins:batch,marker_ids:columnIds()}});if(res.error)throw res.error;(res.data?.records||[]).forEach(r=>{const old=cache.get(String(r.pams_pin))||{};cache.set(String(r.pams_pin),{...old,...r})});Object.entries(res.data?.markers||{}).forEach(([pin,values])=>markerCache.set(String(pin),{...(markerCache.get(String(pin))||{}),...(values||{})}))}await ensureFloodForRows(pins);paintTable();if(mapOn)paintMap();if(note){note.textContent=' · fields checked';setTimeout(()=>{if(note)note.textContent=''},1800)}}catch(e){if(window.WatchdogAgentSafety)window.WatchdogAgentSafety.report('workbench-auto-hydrate',e);if(note)note.textContent=' · some source data unavailable'}finally{if(note)note.classList.remove('dw-hydrating');pending=false}}
-function paintTable(){const ids=columnIds();$$('#dw-rows tr').forEach(tr=>{const cb=$('[data-row]',tr);if(!cb)return;const pin=cb.dataset.row,r=cache.get(pin);if(!r)return;r.__markers={...(r.__markers||{}),...(markerCache.get(pin)||{})};const cells=$$('td',tr).slice(1);ids.forEach((id,i)=>{const cell=cells[i];if(!cell)return;const v=format(id,direct(id,r));if(v!=null){cell.textContent=v;cell.title=v;cell.classList.remove('dw-source-missing')}else{const current=cell.textContent.trim();if(current===''||current==='Not connected'||current==='Not available'){cell.textContent='Not available';cell.classList.add('dw-source-missing')}})})}
+function paintTable(){const ids=columnIds();$$('#dw-rows tr').forEach(tr=>{const cb=$('[data-row]',tr);if(!cb)return;const pin=cb.dataset.row,r=cache.get(pin);if(!r)return;r.__markers={...(r.__markers||{}),...(markerCache.get(pin)||{})};const cells=$$('td',tr).slice(1);ids.forEach((id,i)=>{const cell=cells[i];if(!cell)return;const v=format(id,direct(id,r));if(v!=null){cell.textContent=v;cell.title=v;cell.classList.remove('dw-source-missing')}else{const current=cell.textContent.trim();if(current===''||current==='Not connected'||current==='Not available'){cell.textContent='Not available';cell.classList.add('dw-source-missing')}}})})}
 function ensureHydrateNote(){if($('#dw-hydrate-note'))return;const count=$('#dw-count');if(!count)return;const s=document.createElement('span');s.id='dw-hydrate-note';count.insertAdjacentElement('afterend',s)}
 function ensureMapUi(){if($('#dw-map-toggle'))return;const toolbar=$('.dw-toolbar');if(!toolbar)return;const b=document.createElement('button');b.id='dw-map-toggle';b.type='button';b.innerHTML='<i class="fas fa-map-location-dot"></i> Map';const fields=$('#dw-fields');fields?fields.insertAdjacentElement('afterend',b):toolbar.prepend(b);const wrap=$('.dw-grid-wrap');if(!wrap)return;const pane=document.createElement('aside');pane.id='dw-map-panel';pane.className='dw-map-panel';pane.hidden=true;pane.innerHTML='<header><div><span>MAP VIEW</span><b id="dw-map-count">0 mapped</b></div><button id="dw-map-close" type="button" aria-label="Close map"><i class="fas fa-xmark"></i></button></header><div id="dw-map"></div><small>Map points use current parcel coordinates. Click a point to select the matching Workbench row.</small>';wrap.parentElement.insertBefore(pane,wrap.nextSibling);b.onclick=()=>toggleMap(!mapOn);$('#dw-map-close').onclick=()=>toggleMap(false)}
 function toggleMap(on){mapOn=on;const p=$('#dw-map-panel'),b=$('#dw-map-toggle'),main=$('.dw-main');if(!p)return;p.hidden=!on;b&&b.classList.toggle('on',on);main&&main.classList.toggle('dw-map-open',on);if(on)setTimeout(async()=>{initMap();await hydrate();paintMap()},40)}
diff --git a/property/js/lookup.js b/property/js/lookup.js
index e74f10b..2438845 100644
--- a/property/js/lookup.js
+++ b/property/js/lookup.js
@@ -141,9 +141,34 @@
     b.style.display = 'block';
   }
   window.addEventListener('error', function (e) {
+    // Two things must not raise the red banner.
+    //
+    // 1. Resource failures. An <img>, <script> or <link> that 404s fires an
+    //    error event with no message and the element as the target. A broken
+    //    Street View thumbnail is not a page-stopping fault.
+    // 2. Opaque cross-origin script errors. When a CDN script throws without
+    //    CORS headers the browser hands us the literal string "Script error."
+    //    with an empty filename and lineno 0. We cannot say what broke, the
+    //    page is usually still fine, and telling every visitor the page
+    //    stopped is worse than saying nothing.
+    //
+    // Both are logged so they stay debuggable in the console. Add
+    // crossorigin="anonymous" to a CDN tag to turn case 2 into a real,
+    // reportable error.
+    if (e.target && e.target !== window && e.target.tagName) {
+      console.warn('Resource failed to load:', e.target.src || e.target.href || e.target.tagName);
+      return;
+    }
+    var opaque = !e.filename && !e.lineno &&
+      (!e.message || /^script error\.?$/i.test(String(e.message).trim()));
+    if (opaque) {
+      console.warn('Cross-origin script error, details withheld by the browser. ' +
+        'Add crossorigin="anonymous" to the third-party <script> tags to unmask it.');
+      return;
+    }
     showFatal('A script error stopped the page', e.message,
       (e.filename || '').split('/').pop() + ':' + e.lineno);
-  });
+  }, true);
   window.addEventListener('unhandledrejection', function (e) {
     var r = e.reason;
     showFatal('A request failed and was not handled', (r && r.message) || String(r), '');
@@ -1928,153 +1953,6 @@
         : ''));
   }
 
-  // ══════════════════════════════════════════════
-  // WATCHDOG SCORE
-  //
-  // A 0 to 100 read on whether this property looks over-assessed relative to
-  // what the Watchdog Valuation says it is worth, framed the way a NJ tax
-  // appeal actually gets decided.
-  //
-  // Chapter 123: an assessment survives if it sits at or below true market
-  // value times the town ratio times 1.15. Above that upper limit, the county
-  // board is supposed to reduce it. So the score is really "how far past the
-  // upper limit is this assessment".
-  // ══════════════════════════════════════════════
-  function watchdogScore(assessed, val, comps) {
-    // The valuation is derived from the assessment, so comparing the two tells
-    // us nothing. The only real signal available in public data is how THIS
-    // property is assessed relative to genuinely comparable neighbors.
-    //
-    // Peer group: nearby homes of similar vintage and lot size. If this one
-    // carries a materially higher assessment per comparable home than its
-    // peers do, that is the pattern an appeal is built on.
-    if (!assessed || !comps || comps.length < 10 || !val) return null;
-
-    var subjBuilt = (current && current.yearBuilt) ? +current.yearBuilt : 0;
-    var subjAcres = (current && current.acres) ? +current.acres : 0;
-
-    var peers = comps.filter(function (c) {
-      if (!c.assessed || c.assessed < 10000) return false;
-      if (subjBuilt && c.built && Math.abs(c.built - subjBuilt) > 18) return false;
-      if (subjAcres && c.acres && (c.acres < subjAcres * 0.5 || c.acres > subjAcres * 2.0)) return false;
-      return true;
-    });
-    if (peers.length < 8) {
-      peers = comps.filter(function (c) { return c.assessed && c.assessed > 10000; });
-    }
-    if (peers.length < 8) return null;
-
-    var pa = peers.map(function (c) { return c.assessed; });
-    var medPeer = median(pa);
-    if (!medPeer) return null;
-
-    var gap = (assessed - medPeer) / medPeer;      // how far above peers
-    var score = Math.round(50 + Math.max(-45, Math.min(45, gap * 180)));
-
-    // what the assessment would be at the peer median, and the resulting range
-    var fair = medPeer;
-    var upper = medPeer * 1.15;                     // Chapter 123 style cushion
-
-    var band, label, blurb;
-    if (assessed > upper) {
-      band = 'bad'; label = 'Assessed above its peers';
-      blurb = 'Comparable homes nearby, similar vintage and lot size, carry noticeably lower assessments. ' +
-              'That gap is the pattern a New Jersey appeal is built on. It is not proof, because the public record ' +
-              'cannot see condition or upgrades, but it is worth a real look.';
-    } else if (gap > 0.05) {
-      band = 'warn'; label = 'Slightly above peers';
-      blurb = 'A little higher than comparable homes nearby, but inside the cushion New Jersey allows. ' +
-              'An appeal here would need strong comps to succeed.';
-    } else if (gap < -0.10) {
-      band = 'good'; label = 'Assessed below its peers';
-      blurb = 'This property carries a lower assessment than comparable homes nearby. Good news for the tax bill, ' +
-              'and useful to know before you list.';
-    } else {
-      band = 'ok'; label = 'In line with its peers';
-      blurb = 'The assessment tracks closely with comparable homes nearby. Nothing here suggests an appeal.';
-    }
-
-    return {
-      score: score, band: band, label: label, blurb: blurb,
-      peerCount: peers.length, medPeer: medPeer, fair: fair, upper: upper,
-      over: assessed - upper, overPct: gap,
-      market: val.mid, ratio: val.ratio,
-      appealable: assessed > upper
-    };
-  }
-
-  // Annual dollars an appeal down to the fair assessment would save.
-  function appealSavings(sc, tax, assessed) {
-    if (!sc || !tax || !assessed || !sc.appealable) return null;
-    var effRate = tax / assessed;                       // dollars of tax per dollar assessed
-    var target = sc.fair;                               // what it should be
-    var annual = (assessed - target) * effRate;
-    if (annual < 150) return null;                      // not worth anyone's time
-    return { annual: annual, target: target, fiveYear: annual * 5 };
-  }
-
-  function paintScore(sc, tax, assessed) {
-    var host = el('plm-score-sec');
-    if (!host) { return; }
-    if (!sc) { host.innerHTML = ''; return; }
-
-    var COL = { bad: '#c0392b', warn: '#b8972a', ok: '#2e7d52', good: '#1e6b3a' };
-    var col = COL[sc.band] || '#5a6070';
-    var save = appealSavings(sc, tax, assessed);
-
-    // gauge geometry: 180 degree arc
-    var pctOf = Math.max(0, Math.min(100, sc.score)) / 100;
-    var ang = Math.PI * (1 - pctOf);
-    var cx = 130, cy = 118, rad = 92;
-    var nx = cx + rad * Math.cos(ang), ny = cy - rad * Math.sin(ang);
-
-    host.innerHTML =
-      '<h3 class="plm-sec-h">Watchdog Score</h3>' +
-      '<p class="plm-sec-s">How this assessment compares to genuinely similar homes nearby, matched on vintage and lot size. This is the comparison a New Jersey appeal turns on.</p>' +
-      '<div class="score-wrap">' +
-        '<div class="score-gauge">' +
-          '<svg viewBox="0 0 260 150" role="img" aria-label="Watchdog Score ' + sc.score + '">' +
-            '<defs><linearGradient id="scg" x1="0" y1="0" x2="1" y2="0">' +
-              '<stop offset="0%" stop-color="#1e6b3a"/><stop offset="45%" stop-color="#2e7d52"/>' +
-              '<stop offset="68%" stop-color="#b8972a"/><stop offset="100%" stop-color="#c0392b"/>' +
-            '</linearGradient></defs>' +
-            '<path d="M38 118 A92 92 0 0 1 222 118" fill="none" stroke="#eef0f5" stroke-width="17" stroke-linecap="round"/>' +
-            '<path d="M38 118 A92 92 0 0 1 222 118" fill="none" stroke="url(#scg)" stroke-width="17" stroke-linecap="round" opacity="0.9"/>' +
-            '<line x1="' + cx + '" y1="' + cy + '" x2="' + nx.toFixed(1) + '" y2="' + ny.toFixed(1) + '" stroke="#0e2248" stroke-width="4" stroke-linecap="round"/>' +
-            '<circle cx="' + cx + '" cy="' + cy + '" r="8" fill="#0e2248"/>' +
-            '<text x="38" y="142" font-size="10" fill="#8a93a6">Under</text>' +
-            '<text x="222" y="142" font-size="10" fill="#8a93a6" text-anchor="end">Over</text>' +
-          '</svg>' +
-          '<div class="score-num" style="color:' + col + '">' + sc.score + '</div>' +
-          '<div class="score-lbl" style="color:' + col + '">' + esc(sc.label) + '</div>' +
-        '</div>' +
-        '<div class="score-body">' +
-          '<p>' + esc(sc.blurb) + '</p>' +
-          '<div class="score-rows">' +
-            '<div class="score-row"><span>This assessment</span><b>' + money(assessed) + '</b></div>' +
-            '<div class="score-row"><span>Median of ' + sc.peerCount + ' comparable homes nearby</span><b>' + money(sc.medPeer) + '</b></div>' +
-            '<div class="score-row"><span>Upper limit before it looks out of line</span><b>' + money(sc.upper) + '</b></div>' +
-            (current.valuation && current.valuation.ratioSource === 'official'
-              ? '<div class="score-row"><span>Official ratio, tax year ' + current.valuation.ratioYear + '</span><b>' +
-                (current.valuation.ratio * 100).toFixed(2) + '%</b></div>' : '') +
-            (sc.over > 0 ? '<div class="score-row over"><span>Assessed above the limit by</span><b>' + money(sc.over) + '</b></div>' : '') +
-          '</div>' +
-          (save
-            ? '<div class="score-save">' +
-                '<div class="score-save-n">' + money(save.annual) + '<span>/yr</span></div>' +
-                '<div class="score-save-l">Estimated tax savings if the assessment came down to fair value. ' +
-                  'That is about <b>' + money(save.fiveYear) + '</b> over five years.</div>' +
-                '<button class="plm-rbtn" onclick="plOpenForm(\'appeal\')">See if I can win this appeal</button>' +
-              '</div>'
-            : '<button class="plm-rbtn ghost" style="margin-top:14px;" onclick="plOpenForm(\'value\')">Have John double check this</button>') +
-        '</div>' +
-      '</div>' +
-      '<div class="score-fine">Screening comparison only, built from public assessment records. It cannot see condition, upgrades, or interior finish, ' +
-      'and those are exactly what an assessor weighs. ' +
-      'A real appeal needs current comparable sales and the municipality\u2019s official equalization ratio for the tax year. ' +
-      'Filing deadlines are generally April 1, or May 1 in a town that recently revalued.</div>';
-  }
-
   // ══════════════════════════════════════════════
   // OWNERSHIP TIMELINE
   // ══════════════════════════════════════════════
diff --git a/property/js/search-corrections-v3.js b/property/js/search-corrections-v3.js
index b35c729..704ea1d 100644
--- a/property/js/search-corrections-v3.js
+++ b/property/js/search-corrections-v3.js
@@ -1,23 +1,9 @@
-/* NJW-96 correction v3: statewide search, persisted peer scores, sliders, card actions. */
+/* NJW-96 correction v3: statewide search, sliders, card actions, gated score CTA. */
 (function(){
   'use strict';
-  var PARCEL='https://services2.arcgis.com/XVOqAjTOJ5P6ngMu/ArcGIS/rest/services/Parcels_Composite_NJ_WM/FeatureServer/0/query';
   var GEOCODE='https://geo.nj.gov/arcgis/rest/services/Tasks/NJ_Geocode/GeocodeServer/findAddressCandidates';
-  var SB_URL='https://uvkvaxljhhngydvlrzom.supabase.co';
-  var SB_KEY='sb_publishable_MYX59qCbK3d-21zDfJqkNw_fvmfnexa';
-  var client=null,scoreBusy=false,lastScoreSig='';
 
-  function sb(){
-    if(client)return client;
-    if(window.__njwSB){client=window.__njwSB;return client;}
-    if(!window.supabase)return null;
-    client=window.supabase.createClient(SB_URL,SB_KEY,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true,flowType:'pkce',storageKey:'sb-uvkvaxljhhngydvlrzom-auth-token'}});
-    window.__njwSB=client;
-    return client;
-  }
-  function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];});}
   function pinOf(card){var b=card.querySelector('[data-save-pin]');return b?b.getAttribute('data-save-pin'):'';}
-  function median(a){if(!a.length)return 0;var b=a.slice().sort(function(x,y){return x-y;}),m=b.length>>1;return b.length%2?b[m]:(b[m-1]+b[m])/2;}
   function townFromTitle(){var h=document.getElementById('hd-results-title');return h?h.textContent.replace(/,\s*NJ\s+Property Tax Assessments.*$/i,'').trim():'';}
 
   /* Statewide search: never filter only the already-rendered cards. */
@@ -91,52 +77,63 @@
     });
   }
 
-  function scoreFromPeers(subject,rows){
-    if(!subject||!subject.assessed)return null;
-    var peers=rows.filter(function(c){if(c.pin===subject.pin||!c.assessed||c.assessed<10000)return false;if(subject.built&&c.built&&Math.abs(c.built-subject.built)>18)return false;if(subject.acres&&c.acres&&(c.acres<subject.acres*.5||c.acres>subject.acres*2))return false;return true;});
-    if(peers.length<8)peers=rows.filter(function(c){return c.pin!==subject.pin&&c.assessed>10000;});
-    if(peers.length<8)return null;
-    var med=median(peers.map(function(c){return c.assessed;}));if(!med)return null;
-    var gap=(subject.assessed-med)/med;
-    return {score:Math.round(50+Math.max(-45,Math.min(45,gap*180))),peerCount:peers.length,peerMedian:med};
+  // ── Watchdog Score on search cards ──────────────────────────────
+  //
+  // Search cards no longer display a score. The number that used to sit here
+  // came from an on-demand peer-gap calculation, which is NOT the Watchdog
+  // Score: the real one is the six-component composite in
+  // js/dashboard/tools/watchdog-score.js. Two different numbers under one
+  // name was worse than no number, so the line is now a gated link.
+  //
+  //   no subscription -> "Subscribe", links to the pricing page
+  //   subscribed      -> "View score", links to that property's report
+  //
+  // The composite is deliberately not computed here. It needs municipal
+  // uniformity, appeal, SR1A and revaluation lookups that only load on the
+  // dashboard, and it is a paid signal.
+  function isSubscriber(){
+    try{ return !!(window.NJPTRPlan && window.NJPTRPlan.can && window.NJPTRPlan.can('pro')); }
+    catch(e){ return false; }
   }
 
-  function fetchPeerRows(){
-    // Prefer the parcel list already loaded to build the visible cards —
-    // guaranteed to be the same town/area shown on screen. The independent
-    // map-bounds query below was found to sometimes drift to a different
-    // town's parcels entirely (map bounds not tightly matching the visible
-    // result set), which meant peer PINs could never match any card's PIN.
-    if(window.__njwRows&&window.__njwRows.length){
-      return Promise.resolve(window.__njwRows.map(function(x){
-        return {pin:x.pin||'',town:x.town||'',county:x.county||'',assessed:+x.assessed||0,tax:+x.tax||0,built:+x.built||0,acres:+x.acres||0};
-      }));
+  function scoreLineHTML(pin){
+    if(isSubscriber() && pin){
+      return '<a class="njw-card-score njw-card-score-open" href="/property/home.html?pin='+
+        encodeURIComponent(pin)+'" title="Open the full Watchdog report for this property">'+
+        '<i class="fas fa-shield-dog"></i> Watchdog Score <b>View score</b></a>';
     }
-    var map=window.__njw96HoodMap;if(!map)return Promise.resolve([]);var b=map.getBounds(),latPad=Math.max(.015,(b.getNorth()-b.getSouth())*.65),lonPad=Math.max(.02,(b.getEast()-b.getWest())*.65);
-    var geom={xmin:b.getWest()-lonPad,ymin:b.getSouth()-latPad,xmax:b.getEast()+lonPad,ymax:b.getNorth()+latPad,spatialReference:{wkid:4326}};
-    var p=new URLSearchParams({geometry:JSON.stringify(geom),geometryType:'esriGeometryEnvelope',inSR:'4326',outSR:'4326',spatialRel:'esriSpatialRelIntersects',where:"PROP_CLASS = '2' AND NET_VALUE > 10000",outFields:'PAMS_PIN,MUN_NAME,COUNTY,NET_VALUE,LAST_YR_TX,YR_CONSTR,CALC_ACRE',returnGeometry:'false',resultRecordCount:'2000',f:'json'});
-    return fetch(PARCEL+'?'+p.toString()).then(function(r){return r.json();}).then(function(d){return(d.features||[]).map(function(f){var a=f.attributes||{};return{pin:a.PAMS_PIN||'',town:a.MUN_NAME||'',county:a.COUNTY||'',assessed:+a.NET_VALUE||0,tax:+a.LAST_YR_TX||0,built:+a.YR_CONSTR||0,acres:+a.CALC_ACRE||0};});});
+    return '<a class="njw-card-score njw-card-score-locked" href="/property/pro.html" '+
+      'title="Watchdog Scores are part of a Watchdog subscription">'+
+      '<i class="fas fa-shield-dog"></i> Watchdog Score <b>Subscribe</b></a>';
+  }
+
+  function ensureScoreLine(card){
+    var body=card.querySelector('.hd-body');if(!body)return;
+    var pin=pinOf(card),want=scoreLineHTML(pin);
+    var line=body.querySelector('.njw-card-score');
+    // Replace rather than patch: the element type changes between the two
+    // states (both anchors now, but hrefs and classes differ), and an older
+    // cached div may still be in the DOM from a previous deploy.
+    if(!line){
+      body.insertAdjacentHTML('beforeend',want);
+      return body.querySelector('.njw-card-score');
+    }
+    if(line.outerHTML!==want){
+      line.outerHTML=want;
+      return body.querySelector('.njw-card-score');
+    }
+    return line;
   }
-  function paintScore(card,val,title){var b=card.querySelector('.njw-card-score b');if(!b)return;b.textContent=val==null?'Calculating…':String(Math.round(val));if(title)b.title=title;card.dataset.watchdogScore=val==null?'':String(val);}
-  function ensureScoreLine(card){var body=card.querySelector('.hd-body');if(!body)return;var line=body.querySelector('.njw-card-score');if(!line){line=document.createElement('div');line.className='njw-card-score';line.innerHTML='<i class="fas fa-shield-dog"></i> Watchdog Score <b>Calculating…</b>';body.appendChild(line);}return line;}
 
   function scoreCards(){
-    if(scoreBusy||!sb()||!document.body.classList.contains('hood-on'))return;
-    var cards=Array.prototype.slice.call(document.querySelectorAll('#hd-list .hd-card'));if(!cards.length)return;
-    cards.forEach(function(c){ensureScoreLine(c);});
-    var pins=cards.map(pinOf).filter(Boolean),sig=pins.join('|');if(sig===lastScoreSig&&cards.every(function(c){return c.dataset.njwPeerScoreDone==='1';}))return;lastScoreSig=sig;scoreBusy=true;
-    sb().rpc('get_public_watchdog_score_cache',{p_pins:pins}).then(function(cacheRes){
-      var cache=Object.create(null);if(!cacheRes.error)(cacheRes.data||[]).forEach(function(x){cache[x.pams_pin]=+x.watchdog_score;});
-      cards.forEach(function(c){var v=cache[pinOf(c)];if(v!=null){paintScore(c,v,'Saved Watchdog peer score');c.dataset.njwPeerScoreDone='1';}else paintScore(c,null);});
-      var missing=cards.filter(function(c){return !c.dataset.njwPeerScoreDone;});if(!missing.length)return [];
-      return fetchPeerRows().then(function(rows){
-        var byPin=Object.create(null);rows.forEach(function(x){byPin[x.pin]=x;});var saves=[];
-        missing.forEach(function(card){var subject=byPin[pinOf(card)],sc=scoreFromPeers(subject,rows);if(!sc){paintScore(card,50,'Neutral fallback: insufficient peer evidence');card.dataset.njwPeerScoreDone='1';return;}paintScore(card,sc.score,'Calculated on demand from nearby comparable assessments');card.dataset.njwPeerScoreDone='1';saves.push({pams_pin:subject.pin,watchdog_score:sc.score,town:subject.town,county:subject.county,peer_count:sc.peerCount,peer_median:sc.peerMedian,assessed_value:subject.assessed});});
-        if(saves.length)return sb().rpc('save_public_watchdog_score_cache',{p_rows:saves});return [];
-      });
-    }).catch(function(){cards.forEach(function(c){if(!c.dataset.njwPeerScoreDone){paintScore(c,50,'Neutral fallback');c.dataset.njwPeerScoreDone='1';}});}).finally(function(){scoreBusy=false;var sel=document.getElementById('njw-sort-select');if(sel&&/score/i.test(sel.value))sel.dispatchEvent(new Event('change'));});
+    var cards=Array.prototype.slice.call(document.querySelectorAll('#hd-list .hd-card'));
+    cards.forEach(ensureScoreLine);
   }
 
+  // Plan resolves asynchronously after sign-in, so repaint when it lands.
+  window.addEventListener('njptr:plan-change',function(){ scoreCards(); });
+
+
   function fixScrollEnd(){
     var right=document.querySelector('#pl-hood .hd-right'),art=document.getElementById('njw-search-art');if(!right||!art)return;
     if(art.parentNode!==right)right.appendChild(art);if(art!==right.lastElementChild)right.appendChild(art);
diff --git a/property/js/search-polish-runtime.js b/property/js/search-polish-runtime.js
index 0da341a..9617535 100644
--- a/property/js/search-polish-runtime.js
+++ b/property/js/search-polish-runtime.js
@@ -7,7 +7,6 @@
   var SB_URL = 'https://uvkvaxljhhngydvlrzom.supabase.co';
   var SB_KEY = 'sb_publishable_MYX59qCbK3d-21zDfJqkNw_fvmfnexa';
   var client = null;
-  var scoreMap = Object.create(null);
   var hiddenPins = Object.create(null);
   var townRows = [];
   var townScores = [];
@@ -15,7 +14,7 @@
   var seoBusy = false;
   var seoCacheKey = '';
   var footerPlaceholder = null;
-  var filters = { aMin: '', aMax: '', tMin: '', tMax: '', sMin: '', sMax: '', sort: 'default' };
+  var filters = { aMin: '', aMax: '', tMin: '', tMax: '', sort: 'default' };
 
   function esc(v) {
     return String(v == null ? '' : v).replace(/[&<>"']/g, function (c) {
@@ -62,7 +61,6 @@
     document.querySelectorAll('.njw-card-menu.open').forEach(function (p) { p.classList.remove('open'); });
   }
   function rangeControl(key, label, prefix, min, max, step) {
-    var isScore = key === 's';
     return '<div class="njw-filter-wrap">' +
       '<button class="njw-filter-btn" type="button" data-range-toggle="' + key + '">' + esc(label) + ' <i class="fas fa-chevron-down"></i></button>' +
       '<div class="njw-range-panel" data-range-panel="' + key + '">' +
@@ -72,7 +70,6 @@
           '<em>–</em>' +
           '<label><span>Max</span><div>' + (prefix ? '<b>' + prefix + '</b>' : '') + '<input inputmode="numeric" type="number" min="' + min + '" max="' + max + '" step="' + step + '" data-range-max="' + key + '" placeholder="No max"></div></label>' +
         '</div>' +
-        (isScore ? '<div class="njw-score-scale"><span>0</span><i></i><span>100</span></div>' : '') +
         '<div class="njw-range-actions"><button type="button" data-range-clear="' + key + '">Clear</button><button class="apply" type="button" data-range-apply="' + key + '">Apply</button></div>' +
       '</div></div>';
   }
@@ -90,11 +87,9 @@
       '<div class="njw-search-slot"></div>' +
       rangeControl('a', 'Assessment', '$', 0, 10000000, 10000) +
       rangeControl('t', 'Tax Amount', '$', 0, 100000, 500) +
-      rangeControl('s', 'Watchdog Score', '', 0, 100, 1) +
       '<label class="njw-sort"><span>Sort</span><select id="njw-sort-select">' +
         '<option value="default">Recommended</option><option value="assessmentHigh">Assessment: high to low</option><option value="assessmentLow">Assessment: low to high</option>' +
         '<option value="taxHigh">Tax: high to low</option><option value="taxLow">Tax: low to high</option>' +
-        '<option value="scoreHigh">Watchdog Score: high to low</option><option value="scoreLow">Watchdog Score: low to high</option>' +
       '</select></label>' +
       '<div class="njw-save-slot"></div>';
     hood.insertBefore(bar, split);
@@ -149,9 +144,7 @@
       if (/tax/i.test(s.textContent)) { tax = parseMoney(s.textContent); return true; }
       return false;
     });
-    var pin = pinOf(card);
-    var score = scoreMap[pin] == null ? null : +scoreMap[pin];
-    return { pin: pin, assessed: assessed, tax: tax, score: score };
+    return { pin: pinOf(card), assessed: assessed, tax: tax };
   }
   function visibleByFilters(d) {
     if (hiddenPins[d.pin]) return false;
@@ -159,8 +152,6 @@
     if (filters.aMax !== '' && (d.assessed == null || d.assessed > filters.aMax)) return false;
     if (filters.tMin !== '' && (d.tax == null || d.tax < filters.tMin)) return false;
     if (filters.tMax !== '' && (d.tax == null || d.tax > filters.tMax)) return false;
-    if (filters.sMin !== '' && (d.score == null || d.score < filters.sMin)) return false;
-    if (filters.sMax !== '' && (d.score == null || d.score > filters.sMax)) return false;
     return true;
   }
   function applyCardFilters() {
@@ -176,9 +167,7 @@
       assessmentHigh: function (a,b) { return (cardData(b).assessed || -1) - (cardData(a).assessed || -1); },
       assessmentLow: function (a,b) { return (cardData(a).assessed || Infinity) - (cardData(b).assessed || Infinity); },
       taxHigh: function (a,b) { return (cardData(b).tax || -1) - (cardData(a).tax || -1); },
-      taxLow: function (a,b) { return (cardData(a).tax || Infinity) - (cardData(b).tax || Infinity); },
-      scoreHigh: function (a,b) { return (cardData(b).score == null ? -1 : cardData(b).score) - (cardData(a).score == null ? -1 : cardData(a).score); },
-      scoreLow: function (a,b) { return (cardData(a).score == null ? Infinity : cardData(a).score) - (cardData(b).score == null ? Infinity : cardData(b).score); }
+      taxLow: function (a,b) { return (cardData(a).tax || Infinity) - (cardData(b).tax || Infinity); }
     }[filters.sort];
     if (!fn) return;
     var desired = cards.slice().sort(fn);
@@ -195,31 +184,23 @@
     var html = '<strong>' + n.toLocaleString() + ' properties</strong>';
     if (count.innerHTML !== html) count.innerHTML = html;
   }
-  function paintScores(cards) {
-    cards.forEach(function (card) {
-      var pin = pinOf(card), score = scoreMap[pin];
-      card.dataset.watchdogScore = score == null ? '' : score;
-      // Visible score line is owned by search-corrections-v3.js. Writing it
-      // here too caused a flicker/overwrite race between the two scripts,
-      // where this file's slower fetch would blank out a good number v3
-      // had just painted. This file still tracks scoreMap internally so the
-      // Watchdog Score filter and sort options keep working.
-    });
+  // Score filtering and sorting were removed with the peer-comp score. Search
+  // cards no longer carry a Watchdog Score at all: the real one is the
+  // six-component composite on the report page, and it is a paid signal. A
+  // filter driven by numbers the visitor cannot see was worse than no filter.
+  function clearStaleScores(cards) {
+    // Older deploys stamped a peer-comp score onto each card. Remove it so no
+    // stale attribute is left for anything downstream to read.
+    cards.forEach(function (card) { delete card.dataset.watchdogScore; });
   }
   function hydrateCards() {
     var host = document.getElementById('hd-list');
     if (!host) return;
     var cards = Array.prototype.slice.call(host.querySelectorAll('.hd-card'));
     if (!cards.length) return;
-    cards.forEach(enhanceCard); paintScores(cards);
-    var pins = cards.map(pinOf).filter(Boolean);
-    var missing = pins.filter(function (pin) { return !Object.prototype.hasOwnProperty.call(scoreMap, pin); });
-    if (!missing.length || !sb()) { applyCardFilters(); return; }
-    missing.forEach(function (pin) { scoreMap[pin] = null; });
-    sb().rpc('get_public_property_watchdog_scores', { p_pins: missing }).then(function (r) {
-      if (!r.error) (r.data || []).forEach(function (x) { scoreMap[x.pams_pin] = +x.watchdog_score; });
-      paintScores(cards); applyCardFilters();
-    }).catch(function () { applyCardFilters(); });
+    cards.forEach(enhanceCard);
+    clearStaleScores(cards);
+    applyCardFilters();
   }
   function enhanceCard(card) {
     if (card.dataset.njwPolished === '1') return;
