name: Transaction tax sale route contract

on:
  pull_request:
    paths:
      - 'supabase/functions/transaction-tax-sale-route/**'
      - 'supabase/functions/transaction-evidence-sweep/production-state-evidence-bootstrap.ts'
      - 'property/tests/transaction-tax-sale-route-contract.mjs'
      - '.github/workflows/transaction-tax-sale-route-contract.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node property/tests/transaction-tax-sale-route-contract.mjs
