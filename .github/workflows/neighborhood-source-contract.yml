name: Neighborhood Trends source contract

on:
  push:
    branches:
      - agent/neighborhood-source-build-20260820
    paths:
      - property/scripts/audit_neighborhood_trends.py
      - property/scripts/build_neighborhood_trends.py
      - .github/workflows/neighborhood-source-contract.yml

permissions:
  contents: write

jobs:
  inspect-source:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install workbook reader
        run: python -m pip install --disable-pip-version-check requests openpyxl
      - name: Inspect official 2026 DCA workbook
        run: python property/scripts/audit_neighborhood_trends.py --output property/data/neighborhood-trends-diagnostic.json
      - name: Build governed municipal source artifact
        run: python property/scripts/build_neighborhood_trends.py --output property/data/neighborhood-trends.json
      - name: Commit governed source artifacts
        run: |
          git config user.name "Watchdog Source Automation"
          git config user.email "actions@users.noreply.github.com"
          git add -- property/data/neighborhood-trends-diagnostic.json property/data/neighborhood-trends.json
          if git diff --cached --quiet; then
            echo "No source artifact changes"
          else
            git commit -m "[source-build] Build 2026 DCA Neighborhood Trends data"
            git push origin HEAD:${GITHUB_REF_NAME}
          fi
