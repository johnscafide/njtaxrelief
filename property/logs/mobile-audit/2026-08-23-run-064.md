# Watchdog Mobile Audit — Run 064

**Timestamp:** 2026-08-23 08:41 ET  
**Pages:** `/property/professionals/`; `/property/real-estate-agents/`  
**Areas audited:** Professional-library mobile typography/token normalization, hero/action touch sizing, jump navigation, cards/resources, footer readability, narrow-phone overflow, safe-area handling, and bounded rollout to the first individual profession guide

## Professional library finding
Run 063 restored the professional-library primary navigation on mobile, but the remaining hub still inherited several 10–11px operational labels and compact interaction targets from the desktop-first professional-guide stylesheet. The remaining review also needed explicit narrow-phone behavior for hero actions, long labels, resource cards, footer copy and content wrapping.

## Typography/token normalization
Run 064 normalized the reviewed professional surfaces against the existing Watchdog rem readability scale without redesigning the public/editorial visual language.

- Reused the canonical readability scale through `var(--fs-*, fallback-rem)` references in the mobile layer.
- Raised operational microcopy such as eyebrows, resource labels, card actions, plan pills and metadata to a 12px mobile floor.
- Kept primary card/resource/table/check copy at 14px and strong labels at 15px.
- Raised jump/related navigation labels to 13px with 44px minimum targets.
- Raised primary CTA controls to 48px with 14px labels.
- Preserved the existing professional-guide typefaces, colors, cards, editorial hierarchy and desktop design.

## Professional library mobile change
- Extended `property/css/professionals-mobile-audit.css` from the Run 063 navigation fix into a complete hub readability/touch/overflow pass.
- Added scroll snapping and overscroll containment to the horizontal jump/navigation rows.
- Added 44–48px touch targets for primary CTAs, jump links and related links.
- Added explicit focus-visible coverage for the newly normalized mobile controls.
- Added `overflow-wrap:anywhere` to long card/resource/check/footer copy to prevent narrow-device spill.
- At `<=420px`, stacked hero actions full-width, tightened the hero heading without making it small, reflowed hypothetical-example labels, and respected bottom safe-area padding.

## Bounded rollout / cascade correction
An initial shared-base `@import` was intentionally removed before certification. Loading the mobile audit stylesheet from the top of `professional-guides.css` would both broaden the change to profession pages not yet reviewed and allow later same-specificity base declarations to win in the cascade.

Final state:
- `/property/professionals/` continues to load `professionals-mobile-audit.css` after the base stylesheet.
- `/property/real-estate-agents/` now explicitly loads the same mobile stylesheet after the base stylesheet.
- Unreviewed profession routes are not silently opted into Run 064 and remain queued for their own audit.
- `professional-guides.css` returned to its pre-run shared-base contents, so desktop and unreviewed routes were not globally changed.

## Real Estate Agents guide change
- Added the reviewed mobile audit stylesheet at `max-width:840px`, after the base professional-guide stylesheet.
- Added `viewport-fit=cover` so the existing safe-area rules can participate on edge-to-edge devices.
- Added `aria-label="Primary"` to the guide's primary navigation.
- The shared mobile layer now keeps all professional-guide destinations reachable, converts the jump navigation into a finger-safe horizontal strip, makes CTAs and related links touch-safe, and normalizes the guide's microcopy/card/resource/table/check typography.

## Verification evidence
- Re-fetched current `main` and each target file SHA immediately before every GitHub write.
- Re-fetched the final shared base stylesheet and confirmed the temporary global `@import` is absent.
- Vercel production deployment for final agent-guide commit `4348366b803c1ed9dc410f5a6c5b8d07d2e7512e` reached `READY`; build completed successfully with only the repository's existing Node ESM-to-CommonJS warning.
- Canonical production `https://www.watchdogindex.com/real-estate-agents/` returned HTTP 200 after deployment and contains `viewport-fit=cover`, the mobile audit stylesheet after the base stylesheet, and the primary-nav accessibility label.
- Canonical production `https://www.watchdogindex.com/property/css/professionals-mobile-audit.css` returned HTTP 200 and contains the Run 063–064 rem-scale, touch-target, focus, wrapping and narrow-phone rules.
- Canonical production `https://www.watchdogindex.com/property/css/professional-guides.css` returned HTTP 200 with no global mobile-audit import, confirming the final rollout remains bounded.
- Source commits: `f18539bc2c5478cefdb0913f71154fe64a638afb`, `cf5fd51a4c0f36d55fef87608002de6b7dc8a416`, corrective bounding commit `968d27876fc5906f9da75fc934b4eda2366df46e`, and final route wiring `4348366b803c1ed9dc410f5a6c5b8d07d2e7512e`.

## Visual-check limitation
The available production/browser integration did not expose an interactive device-width screenshot runner in this execution environment. A local `agent-browser` invocation was attempted but the package runner timed out. Therefore this run does **not** claim independent physical-device or pixel-screenshot certification. The certification below is explicitly scoped to completed source review, cascade analysis, responsive-rule inspection, production build readiness and canonical production route/asset verification.

## Desktop isolation
**PASS.** Run 064 visual rules remain inside `@media (max-width:840px)` / `620px` / `420px`, and the mobile stylesheet is loaded with a mobile media condition only on the reviewed routes. Dashboard, Property Home, professional application shells, entitlements, data logic and unreviewed profession-guide pages were not modified by the final net change.

## Certification status
- `/property/professionals/`: **MOBILE CERTIFIED at completed source-review + production-route/asset verification level.** Run 063 cleared the missing-nav blocker; Run 064 completes the hub typography, touch, overflow, footer and narrow-phone review at this evidence level.
- `/property/real-estate-agents/`: **MOBILE CERTIFIED at completed source-review + production-route/asset verification level.** The route now participates in the reviewed mobile layer with safe-area-aware viewport metadata and production verification.

## Backend/Supabase
No auth, entitlement, RLS, billing, provider, evidence-lineage, data-governance or application-data behavior was changed. Production Supabase was read before work and remained the backend boundary; this run required no schema or runtime mutation.

## Next target
`/property/mortgage-lenders/` — review and, only if supported by evidence, opt the next individual profession guide into the mobile audit layer. Continue sequentially rather than globally applying unreviewed mobile overrides.
