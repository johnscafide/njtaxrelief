# Watchdog Mobile Audit — Run 035

- **Timestamp:** 2026-08-21 07:37 ET
- **Page:** `/property/marketing-studio/audience-review/`
- **Area audited:** Advanced filter reveal, property-detail reveal/close flow, keyboard focus behavior, sticky wizard navigation, safe-area handling.
- **Finding:** The prior mobile pass corrected table density, touch sizing, and typography, but the dynamically revealed Filters panel and property-detail panel still relied on visual disclosure only. Mobile keyboard/switch-control users could open either surface without focus moving into the revealed content, Escape did not dismiss the active disclosure, and closing did not reliably return focus to the invoking control. The shared `<=620px` wizard nav was also sticky at `bottom:0` without route-specific home-indicator clearance.
- **Typography/readability assessment:** Existing Run 034 mobile typography remains within the current certification baseline: primary explanatory copy is generally 14px, form controls 16px, operational labels 12–14px, and primary actions 14–15px. No new sub-11px decision-critical mobile copy was introduced.
- **Preserve vs. redesign decision:** Preserve the existing inline filter and detail architecture because both are structurally appropriate on mobile and avoid an unnecessary modal layer. Improve disclosure semantics and focus behavior instead. Preserve the sticky Back/Next pattern but make it safe-area-aware and enforce 48px controls.
- **Change:** Added `property/js/marketing-studio-audience-review-mobile-a11y.js`, which activates only at `<=720px`. It labels the filter/detail regions, moves focus into newly revealed content, supports Escape dismissal, restores focus to the originating Filters/Inspect control, and adds accessible labels to close controls. Updated the mobile audit CSS so the detail region has visible focus and the wizard nav clears `env(safe-area-inset-bottom)` with 48px / 15px controls. Wired the mobile accessibility layer from the Audience Review route after the existing runtime scripts.
- **Verification evidence:** Re-fetched the branch version of the new JS and confirmed the route includes `/property/js/marketing-studio-audience-review-mobile-a11y.js`. Exact source was syntax-checked with `node --check` successfully. Re-fetched the mobile audit CSS after write. PR #99 head advanced to `67c233f06b0942c159533db0f1d523347dedc845`. Current `main` was separately re-fetched at `07eb07716d5b841d683bf2696eae65f064e391ba`, confirming concurrent production work continues independently.
- **Desktop isolation:** Passed. The new JS exits immediately unless `matchMedia('(max-width: 720px)')` matches. The CSS file is loaded with `media="(max-width: 720px)"`. No desktop CSS/JS behavior, Supabase RPC/data logic, auth, billing, or schema was changed.
- **Certification status:** **IN PROGRESS — not yet Mobile Certified.** Audience Review still needs a final pass over loading/empty/error state presentation, dynamic advanced-filter state after rerenders, and route-wide narrow-phone overflow/critical interaction behavior before certification.
- **Next target:** Finish Audience Review loading/empty/error and dynamic state behavior, then advance to `/property/marketing-studio/recipients/`.
- **Integration note:** Draft PR #99 remains open and non-mergeable against the advancing `main`; no force-rebase or unrelated conflict resolution was attempted during this mobile-only run.
