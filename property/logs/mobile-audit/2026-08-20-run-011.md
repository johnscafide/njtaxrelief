# Watchdog Mobile Responsiveness Audit — Run 011

Program owner: NJW-246
Pages: `/property/dashboard`, `/property/home`
Time: 2026-08-20 07:25 ET
Desktop protection rule: changes must be scoped to mobile/tablet breakpoints or mobile-only code paths unless separately authorized.

## Dashboard final certification pass

### Area audited
Profile menu, notifications, widget settings, invite modal, remaining fixed/overlay surfaces, touch targets, mobile viewport fit, readable text, and dismiss actions.

### Finding
The remaining dashboard overlay layer was viewport-aware but still inherited desktop-dense interaction sizing. In `dashboard.css`, the widget-settings close control `.wd6-wclose` was 34×34px, widget option buttons were 34px high with 9px text, the invite close control `.wd6-x` was 34×34px, notification/profile overlay secondary text was 8–10px, and several overlay actions did not guarantee a 44px mobile touch target.

### Change
Updated `property/css/dashboard/dashboard-mobile-audit.css` under the existing `@media (max-width: 768px)` mobile-only layer:
- Profile/notification overlays now fit inside the dynamic viewport below the 68px mobile header.
- Profile and notification text was raised to a readable mobile scale.
- Read-all, sign-out, notification footer, widget options, widget close, and invite close/actions now use 44px+ touch targets.
- Widget and invite modals now use dynamic viewport max heights and safe-area bottom padding.
- Invite form controls are enlarged for mobile input/tap use.

### Desktop isolation
**PASS.** The dashboard audit stylesheet is loaded only through `media="(max-width: 768px)"`; all added rules are also inside its 768px media block. No desktop dashboard CSS or JS was changed.

### Verification evidence
- `property/css/dashboard/dashboard.css` source review confirmed the 34px widget/invite dismiss controls, 34px option buttons, and 8–10px overlay text.
- `property/css/dashboard/dashboard-mobile-audit.css` now contains the dedicated overlay overrides.
- GitHub source verification confirmed the written mobile overlay rules after commit.
- Commit: `53e6d7d9bb293bce540157eae0c5b91d7cc0271a`.

### Dashboard certification
Across Runs 006–011, the dashboard has now had its mobile shell, primary actions, layout/overflow, portfolio table behavior, map/legend controls, card interaction targets, Add Property modal, sponsor safe areas, Agent Intel overlay, narrow-phone typography, and profile/notification/widget/invite overlays reviewed and corrected.

- Major overlay mobile issue: **CLEARED**
- Desktop untouched: **PASS**
- `/property/dashboard` Mobile Certified: **YES — source-review certification complete**

## Property Home — first pass

### Area audited
Primary off-canvas navigation and safe-area behavior.

### Finding
`home-2027.css` defines the mobile navigation close control at 42×42px, below the 44px minimum used by the certification program. The drawer also used fixed 8px top/bottom insets despite `viewport-fit=cover`, allowing the menu foot/dismiss region to sit too close to notches or the iOS home-indicator area.

### Change
Created `property/css/home/home-mobile-audit.css` and loaded it only at `max-width: 820px` from `/property/home/index.html`.
- Navigation close button is now 44×44px minimum.
- Navigation links and sign-out action retain at least 44px touch height.
- Drawer top/bottom insets now include `env(safe-area-inset-top)` and `env(safe-area-inset-bottom)`.
- Drawer max-height uses `100dvh` so the sheet remains inside the visible mobile viewport.

### Desktop isolation
**PASS.** The new stylesheet is both internally scoped to `@media (max-width: 820px)` and loaded with `media="(max-width: 820px)"` from the page head. Desktop Property Home is unchanged.

### Verification evidence
- `home-2027.css` source review confirmed the 42×42 `.hm27-nav-close` and fixed 8px mobile drawer positioning.
- `home-mobile-audit.css` source verification confirms the new safe-area and touch-target rules.
- `/property/home/index.html` source verification confirms the stylesheet is loaded only at mobile/tablet widths.
- Commits: `9a48401498741af5f170c671173f84fbfa0af6d1`, `1dc244976723f284638a9b0a1c1404be7d46238d`.

### Property Home certification
- Primary mobile navigation dismiss/safe-area issue: **CLEARED**
- Desktop untouched: **PASS**
- Entire `/property/home` Mobile Certified: **NOT YET**

## Next target
Continue `/property/home` with the property selector/action row, profile/notification popovers, mobile Agent Intel sheet, long data cards/accordions, narrow-phone typography, and any horizontal overflow generated by detailed property intelligence modules.
