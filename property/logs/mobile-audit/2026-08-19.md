# Watchdog Mobile Responsiveness Audit — 2026-08-19

Program owner: NJW-246
Cadence: hourly, indefinite
Desktop protection rule: changes must be scoped to mobile/tablet breakpoints or mobile-only code paths unless separately authorized.

## Run 001 — 21:21 ET

### Page
`/property/` — `property/index.html`

### Area audited
Scrolled mobile navigation and sticky address-search behavior.

### Finding
**Major usability defect.** The solid Watchdog navigation (`.wd-nav.solid`) becomes fixed at `top: 0`, while the sticky search (`.ssearch`) was also fixed at `top: 0`. The sticky search uses the higher base z-index, which could cover the mobile navigation after the hero is passed.

### Change
Updated `property/css/public-mobile-nav.css` with mobile-only offsets:
- `<= 940px`: sticky search starts at `top: 72px`.
- `<= 560px`: sticky search starts at `top: 78px` to account for the larger 48px mobile header controls and 15px vertical padding.

### Desktop isolation
**PASS.** New behavior is entirely inside `@media(max-width:940px)` and `@media(max-width:560px)`. Desktop `>= 941px` behavior is unchanged.

### Verification evidence
- Source inspection confirmed `.wd-nav.solid` is fixed and `.ssearch` is fixed.
- Post-write GitHub verification confirmed the new mobile-only rules are present on `main`.
- Commit: `c8ced10a32a1ebfb4a89fb6b6d5a636d86c850e5`.

### Certification
- Major item reviewed: **PASS**
- Major item fixed: **PASS**
- Desktop untouched: **PASS**
- Entire page Mobile Certified: **NOT YET**

The landing page remains in active audit until search/autocomplete, modal/property details, horizontal overflow, typography, touch targets, image behavior, and lower-page content have each been reviewed.

### Next target
Continue `/property/` with the property-search/autocomplete interaction and property-details modal at narrow phone widths, then proceed to the next highest-impact landing-page mobile issue before moving to `/property/dashboard`.
