(function(){
'use strict';
const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>Array.from(r.querySelectorAll(s));
let catalog=null, busy=false, coverage=null;
const enabledTiers=new Set(['standard','agent','pro','pro_plus']);
const enabledStatuses=new Set(['live']);
const STATUS_LABEL={live:'LIVE',planned:'PLANNED',unavailable:'UNAVAILABLE'};
const STATUS_HINT={live:'Connected to a live provider and selectable in the Data Workbench.',planned:'No provider connected yet. Selecting it returns nothing.',unavailable:'Cannot be sourced. Selecting it returns nothing.'};
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const tierLabel=t=>t==='pro_plus'?'PRO+':t==='pro'?'PRO':t==='agent'?'AGENT':'STANDARD';
const categoryLabel=m=>{const raw=String(m?.category||'').toLowerCase(),id=String(m?.id||'').toLowerCase(),s=raw+' '+id;if(/appeal|chapter.?123|uniform/.test(s))return'Appeal & Assessment Equity';if(/flood|environment|wetland|hazard/.test(s))return'Flood & Environmental';if(/permit|construction/.test(s))return'Permits & Construction';if(/owner|mailing|ownership/.test(s))return'Ownership & Mailing';if(/assessment|assessed|valuation/.test(s))return'Assessment & Valuation';if(/(^|\s)tax|tax_/.test(s))return'Property Tax';if(/sale|deed|transfer/.test(s))return'Sales & Deeds';if(/building|structure|dwelling/.test(s))return'Building & Improvements';if(/parcel|lot|block|acre|property\.address|municipality|county|zip|pams/.test(s))return'Parcel & Location';if(/market|comp|price/.test(s))return'Market Intelligence';if(/mortgage|lien|finance|loan/.test(s))return'Finance & Liens';if(/watchdog|score|signal|risk|derived/.test(s))return'Watchdog Intelligence';return'Other Property Data'};
const order=['Parcel & Location','Ownership & Mailing','Assessment & Valuation','Property Tax','Sales & Deeds','Building & Improvements','Appeal & Assessment Equity','Flood & Environmental','Permits & Construction','Market Intelligence','Finance & Liens','Watchdog Intelligence','Other Property Data'];
async function getCoverage(){if(coverage)return coverage;coverage={markers:[]};try{await Promise.resolve(window.njptrAccessReady);const c=window.NJPTRAccess&&window.NJPTRAccess.client?window.NJPTRAccess.client():null;if(c){const r=await c.rpc('get_workbench_provider_coverage');if(!r.error)coverage={markers:(r.data||[]).filter(x=>x.value_status==='live')}}}catch(_){ }return coverage}
async function getCatalog(){if(catalog&&coverage)return catalog;try{const a=await fetch('/property/data/marker-registry.json',{cache:'no-store'});if(a.ok)catalog=await a.json()}catch(_){ }catalog=catalog||{markers:[]};await getCoverage();const byId=new Map((catalog.markers||[]).map(m=>[m.id,m]));for(const x of coverage.markers||[]){const m=byId.get(x.marker_id);if(m){m.provider_status='live';m.provider_key=x.provider_key;m.provider_kind=x.provider_kind;m.bulk_capable=x.bulk_capable;m.last_verified_at=x.last_verified_at}else{catalog.markers.push({id:x.marker_id,label:String(x.marker_id).split('.').pop().replace(/_/g,' '),tier:'pro_plus',category:'other',professions:[],provider_status:'live',provider_key:x.provider_key,provider_kind:x.provider_kind,bulk_capable:x.bulk_capable,last_verified_at:x.last_verified_at})}}return catalog}
function markerMap(){return new Map((catalog?.markers||[]).map(m=>[m.id,m]))}
function updateCounts(host){$$('.dw-field-category',host).forEach(group=>{const all=$$('.dw-field',group),visible=all.filter(x=>!x.hidden),shown=$('.dw-field-category-shown',group),total=$('.dw-field-category-total',group);if(shown)shown.textContent=visible.length+' shown';if(total)total.textContent=all.length+' fields';group.hidden=visible.length===0})}
function applyTierFilter(host){$$('.dw-field',host).forEach(label=>{const tier=label.dataset.fieldTier||'standard',st=label.dataset.fieldStatus||'planned';label.hidden=!enabledTiers.has(tier)||!enabledStatuses.has(st)});updateCounts(host)}
function setOpen(group,open,exclusive){if(exclusive&&open)$$('.dw-field-category.open',group.parentElement).forEach(g=>{if(g!==group){g.classList.remove('open');$('.dw-field-category-toggle',g)?.setAttribute('aria-expanded','false')}});group.classList.toggle('open',open);$('.dw-field-category-toggle',group)?.setAttribute('aria-expanded',open?'true':'false')}
async function organize(){if(busy)return;const host=$('#dw-fields-list');if(!host)return;const rawLabels=$$('.dw-field',host).filter(x=>!x.closest('.dw-field-category'));if(!rawLabels.length)return;busy=true;await getCatalog();const byId=markerMap(),groups=new Map();rawLabels.forEach(label=>{const input=$('[data-field]',label);if(!input)return;const m=byId.get(input.dataset.field)||{id:input.dataset.field,category:'other',tier:'pro_plus'},cat=categoryLabel(m),tier=String(m.tier||'standard'),st=String(m.provider_status||'planned');label.dataset.fieldTier=tier;label.dataset.fieldCategory=cat;label.dataset.fieldStatus=st;if(st==='live'){label.classList.add('dw-field-live');input.disabled=false;if(!$('.dw-field-status',label)){const b=document.createElement('span');b.className='dw-field-status live';b.textContent='LIVE';b.title=STATUS_HINT.live;label.appendChild(b)}}else if(!$('.dw-field-status',label)){const b=document.createElement('span');b.className='dw-field-status '+st;b.textContent=STATUS_LABEL[st]||st.toUpperCase();b.title=(m.provider_note?m.provider_note+' — ':'')+(STATUS_HINT[st]||'');label.appendChild(b)}if(!groups.has(cat))groups.set(cat,[]);groups.get(cat).push(label)});host.innerHTML='';const filters=document.createElement('div');filters.className='dw-field-access-filter';filters.innerHTML='<div><span>ACCESS LEVELS</span><small>Filter the library without changing your plan.</small></div><div class="dw-field-access-options">'+['standard','agent','pro','pro_plus'].map(t=>`<label><input type="checkbox" value="${t}" checked><span>${tierLabel(t)}</span></label>`).join('')+'</div>';host.appendChild(filters);$$('input',filters).forEach(cb=>cb.addEventListener('change',()=>{cb.checked?enabledTiers.add(cb.value):enabledTiers.delete(cb.value);applyTierFilter(host)}));const counts={live:0,planned:0,unavailable:0};rawLabels.forEach(l=>{const k=l.dataset.fieldStatus||'planned';if(k in counts)counts[k]++});const avail=document.createElement('div');avail.className='dw-field-access-filter dw-field-status-filter';avail.innerHTML='<div><span>DATA AVAILABILITY</span><small>Live provider fields are selectable and shown by default.</small></div><div class="dw-field-access-options">'+['live','planned','unavailable'].map(t=>`<label title="${esc(STATUS_HINT[t])}"><input type="checkbox" value="${t}"${t==='live'?' checked':''}><span>${STATUS_LABEL[t]} (${counts[t]})</span></label>`).join('')+'</div>';host.appendChild(avail);$$('input',avail).forEach(cb=>cb.addEventListener('change',()=>{cb.checked?enabledStatuses.add(cb.value):enabledStatuses.delete(cb.value);applyTierFilter(host)}));const searching=String($('#dw-field-search')?.value||'').trim().length>0;[...groups.keys()].sort((a,b)=>{const ai=order.indexOf(a),bi=order.indexOf(b);return(ai<0?999:ai)-(bi<0?999:bi)||a.localeCompare(b)}).forEach(cat=>{const section=document.createElement('section');section.className='dw-field-category'+(searching?' open':'');section.innerHTML=`<button type="button" class="dw-field-category-toggle" aria-expanded="${searching?'true':'false'}"><span class="dw-field-category-title"><b>${esc(cat)}</b><small class="dw-field-category-total">${groups.get(cat).length} fields</small></span><span class="dw-field-category-shown">${groups.get(cat).length} shown</span><i class="fas fa-chevron-down" aria-hidden="true"></i></button><div class="dw-field-category-body"></div>`;const body=$('.dw-field-category-body',section);groups.get(cat).forEach(label=>body.appendChild(label));$('.dw-field-category-toggle',section).addEventListener('click',()=>setOpen(section,!section.classList.contains('open'),!searching));host.appendChild(section)});applyTierFilter(host);host.dataset.fieldLibrary='categorized';busy=false}
function watch(){const panel=$('#dw-field-panel');if(!panel)return false;const mo=new MutationObserver(()=>setTimeout(organize,0));mo.observe(panel,{childList:true,subtree:true});document.addEventListener('click',e=>{if(e.target.closest('#dw-fields'))setTimeout(organize,0)});document.addEventListener('input',e=>{if(e.target.id==='dw-field-search')setTimeout(organize,0)});setTimeout(organize,0);return true}
function start(){if(watch())return;const mo=new MutationObserver(()=>{if(watch())mo.disconnect()});mo.observe(document.documentElement,{childList:true,subtree:true})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();
