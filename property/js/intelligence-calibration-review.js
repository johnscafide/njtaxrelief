(function(){
'use strict';
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>Array.from(r.querySelectorAll(s));
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let client=null,sets=[],activeSet=null,cases=[],cursor=0,factCache=new Map(),selectedClass='';
const markerIds=['property.assessed_value','property.land_assessment','property.improvement_assessment','property.annual_tax','property.sale_price','property.sale_date','property.municipality','property.county'];
const reasonOptions=[['','No reason selected'],['evidence_supports_priority','Evidence independently supports review'],['normal_or_explained_change','Normal / explained property change'],['stale_information','Evidence appears stale'],['weak_signal','Signal is too weak'],['missing_evidence','Important evidence is missing'],['wrong_comparison','Comparison / cohort does not fit'],['permit_context','Permit context changes the interpretation'],['closing_not_material','Closing issue is not material'],['other','Other']];
function toast(msg){const t=$('#pl-toast');if(!t)return;t.textContent=msg;t.style.display='block';clearTimeout(window.__crToast);window.__crToast=setTimeout(()=>t.style.display='none',4200)}
async function call(action,extra={}){const r=await client.functions.invoke('intelligence-calibration-admin',{body:{action,...extra}});if(r.error)throw r.error;if(r.data?.error)throw new Error(r.data.error);return r.data||{}}
function num(v,d=2){const n=Number(v);return Number.isFinite(n)?n.toLocaleString('en-US',{maximumFractionDigits:d}):'—'}
function money(v){const n=Number(v);return Number.isFinite(n)?n.toLocaleString('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}):'—'}
function pct(v){const n=Number(v);return Number.isFinite(n)?`${Math.round(n*100)}%`:'—'}
function scorePct(v){const n=Number(v);return Number.isFinite(n)?`${Math.round(n)}%`:'—'}
function labelName(v){return v==='review_priority'?'Priority':v==='not_priority'?'Not priority':v==='uncertain'?'Uncertain':'Unreviewed'}
function predictedName(v){return v==='review_priority'?'Priority':v==='not_priority'?'Not priority':v==='insufficient_evidence'?'Insufficient evidence':'—'}
function reviewedCount(){return cases.filter(c=>c.expected_class&&c.expected_class!=='unreviewed').length}
function queueSets(){return sets.filter(s=>Number(s.case_count||0)>0&&s.status!=='retired')}
function renderOverall(){const qs=queueSets(),total=qs.reduce((n,s)=>n+Number(s.case_count||0),0),open=qs.reduce((n,s)=>n+Number(s.unreviewed_count||0),0);$('#cr-overall').innerHTML=`<b>${total-open} / ${total}</b><span>independent reviews complete</span>`}
function renderSetOptions(){const select=$('#cr-set'),qs=queueSets();select.innerHTML=qs.map(s=>`<option value="${esc(s.id)}" ${activeSet===s.id?'selected':''}>${esc(s.name)} · ${Number(s.unreviewed_count||0)} left</option>`).join('');select.onchange=()=>loadSet(select.value)}
function renderProgress(){const total=cases.length,done=reviewedCount(),percent=total?Math.round(done/total*100):0;$('#cr-progress').innerHTML=`<span>${done}/${total} reviewed</span><span class="cr-progress-bar" aria-hidden="true"><i style="width:${percent}%"></i></span><span>${percent}%</span>`}
function currentCase(){if(!cases.length)return null;cursor=Math.max(0,Math.min(cursor,cases.length-1));return cases[cursor]}
function firstOpenIndex(){const i=cases.findIndex(c=>(c.expected_class||'unreviewed')==='unreviewed');return i<0?cases.length:i}
async function semanticFacts(c){const pin=String(c.pams_pin||'').trim();if(!pin)return null;if(factCache.has(pin))return factCache.get(pin);const r=await client.functions.invoke('intelligence-semantic-context',{body:{pams_pins:[pin],packs:[],marker_ids:markerIds}});if(r.error)throw r.error;factCache.set(pin,r.data||null);return r.data||null}
function markerValue(m){if(!m||m.state!=='available')return'Not available';if(/tax|price|assess/i.test(String(m.id||''))&&Number.isFinite(Number(m.value)))return money(m.value);return typeof m.value==='number'?num(m.value,4):String(m.value??'—')}
function factsMarkup(data){const snap=Array.isArray(data?.snapshots)?data.snapshots[0]:null,items=Array.isArray(snap?.markers)?snap.markers:[],unresolved=Array.isArray(data?.marker_request?.unresolved)?data.marker_request.unresolved:[];if(!snap)return'<div class="cr-note">No governed semantic property snapshot resolved. Review only the evidence that is actually available; do not fill gaps from memory.</div>';return `<div class="cr-fact-grid">${items.map(m=>`<article class="cr-fact ${m.state==='available'?'':'missing'}"><span>${esc(m.label||m.id)}</span><b>${esc(markerValue(m))}</b><small>${esc(m.source||m.source_id||m.state||'No source value')} · ${esc(m.truth_class||'missing')} · authority ${Number(m.authority_rank||0)}/100</small>${m.conflict_state==='conflict'?'<em>Conflicting governed observations exist; canonical authority shown.</em>':''}</article>`).join('')}</div>${unresolved.length?`<div class="cr-unresolved"><b>Unresolved requested markers</b><ul>${unresolved.map(x=>`<li>${esc(x.id)} · ${esc(x.reason)}</li>`).join('')}</ul></div>`:''}<div class="cr-hash">Facts hash: <code>${esc(snap.facts_hash||snap.snapshot_hash||'not recorded')}</code></div>`}
function evidenceMarkup(c){const snapshot=c.evidence_snapshot||{},evidence=Array.isArray(snapshot.evidence)?snapshot.evidence:[],missing=Array.isArray(snapshot.missing_evidence)?snapshot.missing_evidence:[];return `<div class="cr-evidence-list">${evidence.map(e=>`<article class="cr-evidence"><div><b>${esc(e.signal_id||'Signal')}</b><span>${esc(e.role||'evidence')}</span></div><p>Governed source value: <strong>${esc(num(e.value,6))}</strong>${e.cohort?.sample_size?` · peer sample ${Number(e.cohort.sample_size)}`:''}</p><small>${esc(e.explanation||e.source_key||'Governed deterministic evidence')}${e.observed_at?` · observed ${esc(new Date(e.observed_at).toLocaleDateString())}`:''}</small></article>`).join('')||'<div class="cr-note">No scored model evidence is present for this case.</div>'}</div>${missing.length?`<div class="cr-missing"><b>Missing / rejected evidence</b>${missing.map(e=>`<article><strong>${esc(e.signal_id||'Signal')}</strong><span>${esc(e.reason||'missing')}</span></article>`).join('')}</div>`:''}`}
function reasonMarkup(){return reasonOptions.map(([v,l])=>`<option value="${esc(v)}">${esc(l)}</option>`).join('')}
function renderCase(c,facts){selectedClass='';const done=(c.expected_class||'unreviewed')!=='unreviewed',r=c.review||{};$('#cr-stage').innerHTML=`<article class="cr-case"><header class="cr-case-head"><div><span>${esc(activeSetObject()?.model_key||'MODEL')} · VERSION ${esc(activeSetObject()?.model_version||'—')}</span><h2>${esc(c.property_address||c.pams_pin||c.case_key||'Calibration case')}</h2><p>${esc(c.pams_pin||c.case_key||'')} ${done?`· previously labeled ${esc(labelName(c.expected_class))}`:'· independent review pending'}</p></div><div class="cr-case-count">Case ${cursor+1} of ${cases.length}</div></header><div class="cr-grid"><div><section class="cr-panel"><div class="cr-panel-head"><div><span>STEP 1 · GOVERNED SOURCE FACTS</span><h3>What the property record supports</h3></div><small>Observed/source facts are evidence. Missing values remain missing.</small></div>${factsMarkup(facts)}${c.source_notes?`<div class="cr-note"><b>Case provenance</b><br>${esc(c.source_notes)}</div>`:''}</section><section class="cr-panel"><div class="cr-panel-head"><div><span>STEP 2 · DETERMINISTIC EVIDENCE</span><h3>Signals available to the model</h3></div><small>Model score and predicted class remain hidden until you save your label.</small></div>${evidenceMarkup(c)}</section></div><aside class="cr-panel cr-review"><div class="cr-panel-head"><div><span>STEP 3 · HUMAN LABEL</span><h3>Your independent judgment</h3></div></div><div class="cr-review-intro">Would this evidence independently justify putting the property in this model's review queue? Do not infer seller intent, value, profit, legal outcome, urgency, or facts that are not shown.</div><div class="cr-verdicts"><button class="cr-verdict" type="button" data-class="review_priority"><i class="fas fa-flag"></i><span><b>Priority</b><span>Evidence supports review.</span></span></button><button class="cr-verdict" type="button" data-class="not_priority"><i class="fas fa-minus"></i><span><b>Not priority</b><span>Evidence does not justify review.</span></span></button><button class="cr-verdict" type="button" data-class="uncertain"><i class="fas fa-question"></i><span><b>Uncertain</b><span>Evidence is insufficient or ambiguous.</span></span></button></div><div class="cr-form"><label>Optional reason<select id="cr-reason">${reasonMarkup()}</select></label><label>Optional note<textarea id="cr-notes" maxlength="3500" placeholder="What evidence drove your decision?"></textarea></label></div><div class="cr-actions"><button class="cr-skip" type="button" id="cr-skip">Skip for now</button><button class="cr-save" type="button" id="cr-save" disabled>Save independent label</button></div>${done?comparisonMarkup(c):'<div id="cr-after"></div>'}</aside></div></article>`;$$('.cr-verdict').forEach(b=>b.onclick=()=>{selectedClass=b.dataset.class;$$('.cr-verdict').forEach(x=>x.classList.toggle('on',x===b));$('#cr-save').disabled=false});$('#cr-skip').onclick=()=>nextCase(false);$('#cr-save').onclick=()=>saveReview(c)}
function comparisonMarkup(c){const r=c.review||{},outcome=r.review_outcome||'needs_review',good=outcome==='pass',human=labelName(c.expected_class),pred=predictedName(r.predicted_class);return `<div class="cr-after ${outcome==='fail'?'bad':good?'good':''}"><b>${c.expected_class==='uncertain'?'Human review saved as Uncertain':good?'Human label agrees with the model':'Human label disagrees with the model'}</b><span>Human: ${esc(human)} · Watchdog predicted: ${esc(pred)}${r.actual_score==null?'':` · model score ${esc(Number(r.actual_score).toFixed(1))}`}${r.evidence_coverage==null?'':` · evidence coverage ${esc(scorePct(r.evidence_coverage))}`}</span></div>`}
function activeSetObject(){return sets.find(s=>s.id===activeSet)||null}
function structuredNotes(reason,note){const lines=[];if(reason)lines.push(`Reason: ${reason}`);if(note)lines.push(`Reviewer note: ${note}`);return lines.join('\n')}
async function saveReview(c){if(!selectedClass)return;const save=$('#cr-save'),reason=$('#cr-reason')?.value||'',note=$('#cr-notes')?.value?.trim()||'';save.disabled=true;save.innerHTML='<i class="fas fa-circle-notch fa-spin"></i> Saving';try{const result=await call('review_case',{calibration_case_id:c.id,expected_class:selectedClass,review_notes:structuredNotes(reason,note)});const r=c.review||(c.review={});c.expected_class=selectedClass;r.review_outcome=result.review_outcome||'needs_review';r.error_kind=result.error_kind||null;r.review_notes=structuredNotes(reason,note);r.reviewed_at=new Date().toISOString();const predicted=r.predicted_class||'';const human=selectedClass;const negative=predicted==='not_priority'||predicted==='insufficient_evidence';if(!r.review_outcome||r.review_outcome==='needs_review'){r.review_outcome=human==='uncertain'?'needs_review':human==='review_priority'?(predicted==='review_priority'?'pass':'fail'):(negative?'pass':'fail')}$('#cr-after').outerHTML=comparisonMarkup(c)+`<button class="cr-next" id="cr-next" type="button">Save & next case <i class="fas fa-arrow-right"></i></button>`;$('#cr-next').onclick=()=>refreshAfterSave();save.textContent='Saved';renderProgress();toast(`Saved ${labelName(selectedClass)}. Model comparison is now visible.`)}catch(e){save.disabled=false;save.textContent='Save independent label';toast(e?.message||'Could not save this review.')}}
async function refreshAfterSave(){try{const id=activeSet;await loadSet(id,true)}catch(e){toast(e?.message||'Could not load the next case.')}}
function nextCase(allowReviewed=true){if(!cases.length)return;let i=cursor+1;for(;i<cases.length;i++){if(allowReviewed||(cases[i].expected_class||'unreviewed')==='unreviewed'){cursor=i;return loadCurrent()}}toast('No later unreviewed cases in this queue.')}
async function loadCurrent(){const c=currentCase();if(!c)return renderComplete();$('#cr-stage').innerHTML='<div class="cr-loading"><i class="fas fa-circle-notch fa-spin"></i><span>Loading governed source facts…</span></div>';try{const facts=await semanticFacts(c);renderCase(c,facts)}catch(e){$('#cr-stage').innerHTML=`<div class="cr-empty"><i class="fas fa-triangle-exclamation"></i><h2>Source facts unavailable</h2><p>${esc(e?.message||'Semantic Context could not resolve this case.')} Do not guess. Use the full calibration control if deeper investigation is needed.</p></div>`}}
function renderComplete(){const set=activeSetObject(),sm=set?.summary||{};$('#cr-stage').innerHTML=`<div class="cr-complete"><i class="fas fa-circle-check"></i><h2>${esc(set?.name||'Queue')} is fully reviewed.</h2><p>All ${cases.length} cases have independent human labels. The calibration gate is computed separately from those labels${sm.passes_gate?' and currently passes.':'. Open the full calibration control to inspect precision, recall, false-positive rate, and any failed thresholds.'}</p></div>`}
async function loadSet(id,afterSave=false){activeSet=id;$('#cr-stage').innerHTML='<div class="cr-loading"><i class="fas fa-circle-notch fa-spin"></i><span>Loading calibration queue…</span></div>';const d=await call('get_set',{calibration_set_id:id});cases=Array.isArray(d.cases)?d.cases:[];const setIndex=sets.findIndex(s=>s.id===id);if(setIndex>=0){sets[setIndex]={...sets[setIndex],summary:d.summary||sets[setIndex].summary,unreviewed_count:cases.filter(c=>(c.expected_class||'unreviewed')==='unreviewed').length,case_count:cases.length}}renderOverall();renderSetOptions();renderProgress();cursor=firstOpenIndex();if(cursor>=cases.length)return renderComplete();await loadCurrent();if(afterSave)window.scrollTo({top:document.querySelector('.cr-controls')?.offsetTop||0,behavior:'smooth'})}
async function boot(){try{await Promise.resolve(window.njptrAccessReady);client=window.NJPTRAccess?.client?.();if(!client)throw new Error('Supabase client unavailable.');const d=await call('list');sets=Array.isArray(d.sets)?d.sets:[];renderOverall();const qs=queueSets();if(!qs.length){renderSetOptions();$('#cr-stage').innerHTML='<div class="cr-empty"><i class="fas fa-flask"></i><h2>No active calibration queues</h2><p>There are no non-retired calibration sets with cases to review.</p></div>';return}const withOpen=qs.find(s=>Number(s.unreviewed_count||0)>0)||qs[0];activeSet=withOpen.id;renderSetOptions();await loadSet(activeSet)}catch(e){$('#cr-overall').textContent='Calibration unavailable';$('#cr-stage').innerHTML=`<div class="cr-empty"><i class="fas fa-triangle-exclamation"></i><h2>Reviewer could not start</h2><p>${esc(e?.message||'Developer calibration service is unavailable.')}</p></div>`}}
boot();
})();
