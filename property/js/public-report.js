(()=>{'use strict';const $=id=>document.getElementById(id),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const token=new URLSearchParams(location.search).get('t');function render(data){const p=data.version.report_payload||{},fields=Array.isArray(p.fields)?p.fields:[],rows=Array.isArray(p.rows)?p.rows:[],farm=p.farm||{};let body='';if(p.layout==='brief'){body=rows.map(r=>`<section class="pr-brief"><h3>${esc(r.address||'Property')}</h3><small>${esc(r.town||'')}${r.zip?' · '+esc(r.zip):''}</small><div class="pr-metrics">${fields.map(f=>`<div><span>${esc(f.label||f.id)}</span><b>${esc(r.values?.[f.id]??'Not available')}</b></div>`).join('')}</div></section>`).join('')}else{body=`<div class="pr-scroll"><table><thead><tr><th>Property</th>${fields.map(f=>`<th>${esc(f.label||f.id)}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr><td><b>${esc(r.address||'Property')}</b><br><small>${esc(r.town||'')}</small></td>${fields.map(f=>`<td>${esc(r.values?.[f.id]??'Not available')}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`}document.title=(p.title||data.report.title||'Watchdog Report')+' | Watchdog';$('pr-app').innerHTML=`<article class="pr-paper"><header class="pr-head"><div><span>${p.audience==='client'?'CLIENT PROPERTY REPORT':'PROFESSIONAL PROPERTY INTELLIGENCE'}</span><h1>${esc(p.title||data.report.title||'Watchdog Report')}</h1><p>${esc(p.subtitle||'')}</p></div><div class="pr-brand"><b>Watchdog</b><small>NJ property intelligence</small></div></header><section class="pr-summary"><div><b>${Number(farm.match_count||rows.length).toLocaleString()}</b><span>farm matches</span></div><div><b>${rows.length}</b><span>properties in snapshot</span></div><div><b>${fields.length}</b><span>curated data points</span></div><div><b>${new Date(p.generated_at||data.version.created_at).toLocaleDateString()}</b><span>report date</span></div></section>${body}<footer>Shared from Watchdog. This link is a point-in-time report snapshot. Public records and derived analytics can change after publication.</footer></article>`}async function start(){if(!token){$('pr-app').innerHTML='<div class="pr-error">This report link is incomplete.</div>';return}try{const r=await fetch('https://uvkvaxljhhngydvlrzom.supabase.co/functions/v1/public-report-share',{method:'POST',headers:{'Content-Type':'application/json','apikey':'sb_publishable_MYX59qCbK3d-21zDfJqkNw_fvmfnexa'},body:JSON.stringify({token})});const data=await r.json();if(!r.ok)throw new Error(data.error||'Report unavailable');render(data)}catch(e){$('pr-app').innerHTML=`<div class="pr-error"><h1>Report unavailable</h1><p>${esc(e.message||'This shared report could not be opened.')}</p></div>`}}$('pr-print').onclick=()=>window.print();start()})();