(()=>{'use strict';const $=id=>document.getElementById(id),params=new URLSearchParams(location.search),listId=params.get('list');let client,user,map,drawn,polygon=null,lastData=null,list=null;const safe=(e,c)=>window.WatchdogAgentSafety?window.WatchdogAgentSafety.friendlyError(e,c):'Watchdog could not complete that request. Please try again.';function toast(m){const n=$('pl-toast');if(!n)return;n.textContent=m;n.style.display='block';clearTimeout(window.__fm);window.__fm=setTimeout(()=>n.style.display='none',3200)}function classes(){const v=$('fm-class').value;return v?v.split(','):[]}function intelligenceFilters(){const mapIds={watchdog_score_min:'fm-wd-min',watchdog_score_max:'fm-wd-max',tax_pressure_min:'fm-taxp-min',tax_pressure_max:'fm-taxp-max',years_since_sale_min:'fm-hold-min',years_since_sale_max:'fm-hold-max',building_age_min:'fm-age-min',building_age_max:'fm-age-max',overassessment_annual_min:'fm-over-annual',overassessment_pct_min:'fm-over-pct',assessment_reduction_min:'fm-over-reduction'},o={};Object.entries(mapIds).forEach(([k,id])=>{const el=$(id),v=el?el.value.trim():'';if(v!=='')o[k]=Number(v)});if($('fm-over-only')?.checked)o.overassessment_only=true;return o}function hasIntel(){return Object.keys(intelligenceFilters()).length>0}function polygonBody(){return{scope_type:'polygon',scope_value:'Map selection',polygon:{rings:[polygon]},property_classes:classes(),filters:{},intelligence_filters:intelligenceFilters(),limit:250,offset:0,result_offset:0,scan_limit:2000}}function updateState(){const ready=Array.isArray(polygon)&&polygon.length>=4;$('fm-preview').disabled=!ready;$('fm-save').disabled=!ready;$('fm-status').textContent=ready?'Area selected. Preview it to count the matching parcels.':'Search for an address or town, then draw the exact farm area.'}function latLngsToRing(layer){const ll=layer.getLatLngs(),arr=Array.isArray(ll[0])&&Array.isArray(ll[0][0])?ll[0][0]:ll[0],ring=arr.map(p=>[Number(p.lng),Number(p.lat)]);if(ring.length&&String(ring[0])!==String(ring[ring.length-1]))ring.push(ring[0]);return ring}function addSavedPolygon(ring){if(!Array.isArray(ring)||ring.length<4)return;drawn.clearLayers();const layer=L.polygon(ring.map(x=>[Number(x[1]),Number(x[0])])).addTo(drawn);polygon=ring;map.fitBounds(layer.getBounds(),{padding:[30,30]});updateState()}async function preview(){const b=$('fm-preview'),old=b.innerHTML,intel=hasIntel();b.disabled=true;b.innerHTML=`<i class="fas fa-circle-notch fa-spin"></i> ${intel?'Scanning intelligence':'Checking parcels'}`;try{const fn=intel?'farm-intelligence-scan':'farm-map-query',r=await client.functions.invoke(fn,{body:polygonBody()});if(r.error)throw r.error;lastData=r.data;const n=Number(intel?(r.data?.match_count||0):(r.data?.count||0));$('fm-status').textContent=intel?`${n.toLocaleString()} parcels match the selected area + Watchdog intelligence rules.`:`${n.toLocaleString()} matching parcels inside the selected map area.`;const card=$('fm-preview-card');card.hidden=false;card.innerHTML=intel?`<b>${n.toLocaleString()} intelligence matches</b><span>${Number(r.data?.scanned_count||0).toLocaleString()} candidate parcels scanned${r.data?.capacity_limited?' · plan capacity applied':''}${r.data?.truncated?' · scan cap reached':''}. Save this as a working intelligence farm.</span>`:`<b>${n.toLocaleString()} matching parcels</b><span>${classes().length?classes().join(', ')+' property class filter':'All supported property classes'} · first ${Math.min(250,n).toLocaleString()} ready to load after saving.</span>`}catch(e){if(window.WatchdogAgentSafety)window.WatchdogAgentSafety.report('farm-map-preview',e);toast(safe(e,'farm-map-preview'))}finally{b.disabled=false;b.innerHTML=old}}async function save(){if(!polygon)return;const name=$('fm-name').value.trim()||'Map-drawn farm',b=$('fm-save'),old=b.innerHTML;b.disabled=true;b.innerHTML='<i class="fas fa-circle-notch fa-spin"></i> Saving';try{const ints=intelligenceFilters(),record={user_id:user.id,name,scope_type:'polygon',scope_value:'Map selection',criteria:{property_classes:classes(),filters:{},intelligence_filters:ints,polygon:{rings:[polygon]}},monitored:true,last_count:Number((lastData?.match_count??lastData?.count)??0)||null};let x;if(listId&&list)x=await client.from('agent_dynamic_lists').update({...record,updated_at:new Date().toISOString()}).eq('id',listId).select().single();else x=await client.from('agent_dynamic_lists').insert(record).select().single();if(x.error)throw x.error;location.href='/property/market-list?id='+encodeURIComponent(x.data.id)}catch(e){if(window.WatchdogAgentSafety)window.WatchdogAgentSafety.report('farm-map-save',e);b.disabled=false;b.innerHTML=old;toast(safe(e,'farm-map-save'))}}async function geocode(){const input=$('fm-place'),q=input.value.trim();if(!q)return;const b=$('fm-find');b.disabled=true;b.innerHTML='<i class="fas fa-circle-notch fa-spin"></i>';try{const d=window.NJPlaceAutocomplete?await window.NJPlaceAutocomplete.resolve(input):null;if(!d)throw new Error('NJ location service unavailable');input.value=d.text;map.setView([d.lat,d.lng],15,{animate:true});L.circleMarker([d.lat,d.lng],{radius:7,weight:2,fillOpacity:.8}).addTo(map).bindTooltip(d.text,{permanent:false}).openTooltip()}catch(e){if(window.WatchdogAgentSafety)window.WatchdogAgentSafety.report('farm-map-geocode',e);toast(safe(e,'farm-map-geocode'))}finally{b.disabled=false;b.innerHTML='<i class="fas fa-magnifying-glass"></i>'}}function initMap(){map=L.map('fm-map',{zoomControl:true}).setView([40.06,-74.50],8);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap'}).addTo(map);drawn=new L.FeatureGroup().addTo(map);const control=new L.Control.Draw({edit:{featureGroup:drawn},draw:{polyline:false,circle:false,circlemarker:false,marker:false,polygon:{allowIntersection:false,showArea:true},rectangle:true}});map.addControl(control);map.on(L.Draw.Event.CREATED,e=>{drawn.clearLayers();drawn.addLayer(e.layer);polygon=latLngsToRing(e.layer);lastData=null;updateState()});map.on(L.Draw.Event.EDITED,e=>{e.layers.eachLayer(layer=>{polygon=latLngsToRing(layer)});lastData=null;updateState()});map.on(L.Draw.Event.DELETED,()=>{polygon=null;lastData=null;$('fm-preview-card').hidden=true;updateState()});setTimeout(()=>map.invalidateSize(),80)}async function loadSavedFarm(){if(!listId)return;const r=await client.from('agent_dynamic_lists').select('*').eq('id',listId).single();if(r.error)throw r.error;list=r.data;$('fm-name').value=list.name||'';const c=list.criteria||{},cls=Array.isArray(c.property_classes)?c.property_classes.join(','):'';$('fm-class').value=cls||'';const ints=c.intelligence_filters||{},mapIds={watchdog_score_min:'fm-wd-min',watchdog_score_max:'fm-wd-max',tax_pressure_min:'fm-taxp-min',tax_pressure_max:'fm-taxp-max',years_since_sale_min:'fm-hold-min',years_since_sale_max:'fm-hold-max',building_age_min:'fm-age-min',building_age_max:'fm-age-max',overassessment_annual_min:'fm-over-annual',overassessment_pct_min:'fm-over-pct',assessment_reduction_min:'fm-over-reduction'};Object.entries(mapIds).forEach(([k,id])=>{if(ints[k]!=null&&$(id))$(id).value=ints[k]});if($('fm-over-only'))$('fm-over-only').checked=ints.overassessment_only===true;const ring=c.polygon?.rings?.[0];if(ring)addSavedPolygon(ring);$('fm-db-tab').href='/property/market-list?id='+encodeURIComponent(list.id);$('fm-db-tab').innerHTML='<i class="fas fa-table"></i> Farm database';$('fm-report-tab').href='/property/report-studio?list='+encodeURIComponent(list.id);$('fm-report-tab').classList.remove('disabled');$('fm-report-tab').title='Open Report Studio'}function bindPresets(){document.querySelectorAll('[data-assessment-preset]').forEach(b=>b.onclick=()=>{$('fm-over-only').checked=true;$('fm-over-annual').value=b.dataset.assessmentPreset;lastData=null;toast('Assessment screening preset applied')})}Promise.resolve(window.njptrAccessReady).then(async ctx=>{user=ctx.user;client=window.NJPTRAccess.client();initMap();bindPresets();if(window.NJPlaceAutocomplete)window.NJPlaceAutocomplete.init(document);$('fm-preview').onclick=preview;$('fm-save').onclick=save;$('fm-find').onclick=geocode;$('fm-place').onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();geocode()}};$('fm-place').addEventListener('nj:place-selected',()=>geocode());$('fm-report-tab').addEventListener('click',e=>{if(!listId){e.preventDefault();toast('Save the farm first, then Report Studio can open this exact property set.')}});await loadSavedFarm();updateState()}).catch(e=>{if(window.WatchdogAgentSafety)window.WatchdogAgentSafety.report('farm-map-start',e);toast(safe(e,'farm-map-start'))})})();
