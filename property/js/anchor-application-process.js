(function(){
'use strict';
var form=document.getElementById('wd-anchor-form');
if(!form||!window.WatchdogAnchorVault)return;
var vault=window.WatchdogAnchorVault,db;try{db=vault.supabaseClient();}catch(_){return;}
var APP_KEY='wd_anchor_2025_application_id',TAX_YEAR=2025,PARTIAL='/property/partials/anchor-application-process.html';
var root=null,saveTimer=null,lastObservedSave='',visited={},reuseCandidate=null;
var STEP_ORDER=['welcome','account','vault','profile','disability','route','identity','address','ssn','residence','not-eligible','housing','anc-details','pas-history','property','pas-income','schedule','finish','review','complete'];
var GROUPS={account:['account','vault'],profile:['profile','disability','route'],identity:['identity'],address:['address','ssn','residence','housing','property'],"anc-details":['anc-details','pas-history','pas-income','schedule','finish'],review:['review','complete']};
var SAFE_REUSE=['applicant.first','applicant.middle','applicant.last','spouse.first','spouse.middle','spouse.last','mailing.address','mailing.city','mailing.state','mailing.zip','mailing.municipality_code','oct1.address','oct1.municipality_code','property.block','property.block_suffix','property.lot','property.lot_suffix','property.qualifier'];
function q(s,r){return(r||document).querySelector(s)}function qa(s,r){return Array.prototype.slice.call((r||document).querySelectorAll(s))}
function appId(){try{return String(sessionStorage.getItem(APP_KEY)||'')}catch(_){return''}}
function activeStep(){var n=q('.wd-step.is-active',form);return n&&n.dataset.step||'welcome'}
function pathGet(obj,path){return String(path).split('.').reduce(function(v,k){return v&&Object.prototype.hasOwnProperty.call(v,k)?v[k]:undefined},obj)}
function setSaveState(mode,stamp){if(!root)return;var bar=q('.wd-anchor-savebar',root),label=q('[data-anchor-save-state]',root),time=q('[data-anchor-save-time]',root);bar.classList.toggle('is-saving',mode==='saving');bar.classList.toggle('is-error',mode==='error');label.textContent=mode==='saving'?'Saving encrypted draft…':mode==='error'?'Save needs attention':'Saved securely';if(time)time.textContent=stamp?('Last saved '+stamp):mode==='error'?'Keep this page open and try again.':'Changes save automatically.'}
function markSaving(){if(!appId())return;setSaveState('saving');clearTimeout(saveTimer);saveTimer=setTimeout(confirmSaved,1000)}
async function confirmSaved(){var id=appId();if(!id)return;try{var r=await db.from('anchor_applications').select('updated_at').eq('id',id).maybeSingle();if(r.error||!r.data)throw(r.error||new Error('not_saved'));lastObservedSave=r.data.updated_at||'';var stamp=new Intl.DateTimeFormat('en-US',{hour:'numeric',minute:'2-digit'}).format(new Date(lastObservedSave));setSaveState('saved',stamp)}catch(_){setSaveState('error')}}
async function saveAndExit(){var button=q('[data-anchor-save-exit]',root);button.disabled=true;var id=appId();if(!id){location.href='/anchor/applications/';return}setSaveState('saving');await new Promise(function(resolve){setTimeout(resolve,1100)});await confirmSaved();location.href='/anchor/applications/'}
function updateVisited(){var current=activeStep(),idx=STEP_ORDER.indexOf(current);if(idx>=0)visited[current]=true;try{var prior=JSON.parse(sessionStorage.getItem('wd_anchor_2025_visited')||'{}');Object.keys(prior||{}).forEach(function(k){if(prior[k])visited[k]=true});visited[current]=true;sessionStorage.setItem('wd_anchor_2025_visited',JSON.stringify(visited))}catch(_){}qa('[data-anchor-jump]',root).forEach(function(btn){var target=btn.dataset.anchorJump,steps=GROUPS[target]||[target],isCurrent=steps.indexOf(current)>=0,allowed=steps.some(function(s){return visited[s]})&&STEP_ORDER.indexOf(target)<=idx;btn.classList.toggle('is-current',isCurrent);btn.classList.toggle('is-visited',allowed||isCurrent);btn.disabled=!(allowed||isCurrent)})}
function jumpBack(target){var wanted=GROUPS[target]||[target],attempts=0;function run(){var current=activeStep();if(wanted.indexOf(current)>=0)return updateVisited();var ci=STEP_ORDER.indexOf(current),ti=STEP_ORDER.indexOf(target);if(ti<0||ci<=ti||attempts++>24)return;var back=q('.wd-step.is-active [data-back]',form);if(!back)return;back.click();setTimeout(run,0)}run()}
function clearInline(step){qa('.wd-inline-error',step).forEach(function(n){n.remove()});qa('.wd-inline-invalid',step).forEach(function(n){n.classList.remove('wd-inline-invalid');n.removeAttribute('aria-invalid')})}
function flag(el,msg){if(!el)return;el.classList.add('wd-inline-invalid');el.setAttribute('aria-invalid','true');var note=document.createElement('small');note.className='wd-inline-error';note.textContent=msg;(el.closest('label')||el.parentElement||el).appendChild(note)}
function inlineValidate(){var step=q('.wd-step.is-active',form);if(!step)return;clearInline(step);qa('[required]',step).forEach(function(el){if(el.closest('.is-hidden')||el.closest('[hidden]'))return;if(!String(el.value||'').trim())flag(el,'Complete this field before continuing.')});if(step.dataset.step==='ssn'){qa('input[name$=".ssn"]',step).forEach(function(el){if(String(el.value||'').replace(/\D/g,'').length!==9)flag(el,'Enter all nine digits required by the State form.')})}if(step.dataset.step==='pas-income'){var amounts=qa('input[name^="income_2024."],input[name^="income_2025."]',step);if(amounts.length){amounts.forEach(function(el){if(String(el.value||'').trim()==='')flag(el,'Enter 0 when the correct amount is zero.')});if(amounts.some(function(el){return String(el.value||'').trim()===''})){var status=q('#wd-app-status');if(status){status.textContent='Enter all ten PAS-1 income amounts. Enter 0 where the correct amount is zero.';status.className='wd-app-status is-visible error'}}}}
function installWhy(){['profile','disability','address','ssn','property','pas-income','review'].forEach(function(stepId){var step=q('.wd-step[data-step="'+stepId+'"]',form);if(!step||q('.wd-anchor-why',step))return;var h=q('h2',step)||q('h3',step);if(!h)return;var b=document.createElement('button');b.type='button';b.className='wd-anchor-why';b.textContent='Why are we asking this?';b.dataset.helpStep=stepId;h.insertAdjacentElement('afterend',b)})}
function helpFor(step){if(!root)return;var body=q('[data-anchor-help-body]',root),t=q('template[data-anchor-help-step="'+step+'"]',root)||q('template[data-anchor-help-step="review"]',root);if(body&&t){body.innerHTML='';body.appendChild(t.content.cloneNode(true))}}
function openHelp(step){helpFor(step||activeStep());var drawer=q('.wd-anchor-help-drawer',root),shade=q('.wd-anchor-help-shade',root);drawer.classList.add('is-open');drawer.setAttribute('aria-hidden','false');shade.hidden=false}
function closeHelp(){var drawer=q('.wd-anchor-help-drawer',root),shade=q('.wd-anchor-help-shade',root);drawer.classList.remove('is-open');drawer.setAttribute('aria-hidden','true');shade.hidden=true}
function fieldPresent(name){var el=q('[name="'+name+'"]',form);return !!(el&&String(el.value||'').trim())}
function reviewAudit(){var review=q('.wd-step[data-step="review"]',form);if(!review)return;var card=q('.wd-anchor-review-audit',review);if(!card){var t=q('#wd-anchor-review-audit-template',root);if(t){card=t.content.firstElementChild.cloneNode(true);var warning=q('#wd-review-warning',review);(warning||review.firstElementChild).insertAdjacentElement(warning?'beforebegin':'afterend',card);q('[data-anchor-review-pdf]',card).addEventListener('click',function(){var preview=q('#wd-open-pdf-preview');if(preview)preview.click()})}}if(!card)return;var checks={applicant:fieldPresent('applicant.first')&&fieldPresent('applicant.last'),address:fieldPresent('mailing.address')&&fieldPresent('mailing.city')&&fieldPresent('mailing.zip')&&fieldPresent('mailing.municipality_code'),property:fieldPresent('property.block')&&fieldPresent('property.lot'),income:true,form:!!String((q('#wd-route-badge')||{}).textContent||'').trim()};var pas=qa('input[name^="income_2024."],input[name^="income_2025."]',form);if(pas.length)checks.income=!pas.some(function(el){return String(el.value||'').trim()==='' });Object.keys(checks).forEach(function(key){var row=q('[data-audit="'+key+'"]',card);if(!row)return;row.classList.toggle('is-missing',!checks[key]);q('strong',row).textContent=checks[key]?'Ready':'Review'})}
async function deadline(){var r=await db.from('anchor_program_deadlines').select('deadline_date,source_url,source_title,verified_at').eq('tax_year',TAX_YEAR).eq('active',true).in('form_type',['all','anc-1','pas-1']).order('form_type',{ascending:true}).limit(1);return r.error||!r.data||!r.data.length?null:r.data[0]}
async function filingState(id){var r=await db.from('anchor_application_filing_state').select('filing_status,filed_on,filing_method,updated_at').eq('application_id',id).maybeSingle();return r.error?null:r.data}
async function reminderState(id){var r=await db.from('anchor_application_reminders').select('offset_days,status,scheduled_for').eq('application_id',id);return r.error?[]:(r.data||[])}
function formatDate(value){try{return new Intl.DateTimeFormat('en-US',{month:'long',day:'numeric',year:'numeric'}).format(new Date(value+'T12:00:00'))}catch(_){return value||''}}
async function refreshFiling(center){var id=appId();if(!id)return;var state=await filingState(id),buttons=qa('[data-filing-status]',center);buttons.forEach(function(b){b.classList.toggle('is-active',!!state&&b.dataset.filingStatus===state.filing_status)});var copy=q('[data-filing-status-copy]',center);if(copy){if(!state)copy.textContent='Not marked as filed yet.';else if(state.filing_status==='ready_to_file')copy.textContent='Ready to file.';else if(state.filing_status==='mailed')copy.textContent='Marked mailed'+(state.filed_on?' on '+formatDate(state.filed_on):'')+'.';else if(state.filing_status==='filed_other')copy.textContent='Marked filed'+(state.filed_on?' on '+formatDate(state.filed_on):'')+(state.filing_method?' · '+state.filing_method:'')+'.';else copy.textContent='Not filed yet.'}var rem=await reminderState(id);qa('[data-reminder]',center).forEach(function(box){var off=Number(box.dataset.reminder),row=rem.find(function(x){return Number(x.offset_days)===off});box.checked=!!row&&['scheduled','processing','sent'].indexOf(row.status)>=0;box.disabled=!!state&&['mailed','filed_other'].indexOf(state.filing_status)>=0})}
async function installFilingCenter(){var complete=q('.wd-step[data-step="complete"]',form);if(!complete||q('#wd-anchor-filing-center',complete))return;var t=q('#wd-anchor-filing-center-template',root);if(!t)return;var center=t.content.firstElementChild.cloneNode(true);var first=q('.wd-support-card',complete)||complete.lastElementChild;first?complete.insertBefore(center,first):complete.appendChild(center);var d=await deadline();if(d){var box=q('[data-filing-deadline]',center);box.hidden=false;q('[data-filing-deadline-date]',box).textContent=formatDate(d.deadline_date);var link=q('[data-filing-deadline-source]',box);link.href=d.source_url;link.title=d.source_title||'Official New Jersey source'}q('[data-filing-review]',center).addEventListener('click',function(){var p=q('#wd-open-pdf-preview');if(p)p.click()});qa('[data-filing-status]',center).forEach(function(b){b.addEventListener('click',async function(){var id=appId();if(!id)return;var status=b.dataset.filingStatus,method='';if(status==='filed_other'){var wrap=q('[data-filing-method-wrap]',center);wrap.hidden=false;method=String(q('[data-filing-method]',center).value||'').trim();if(!method){q('[data-filing-status-copy]',center).textContent='Add how you filed, then choose “Filed another way” again.';q('[data-filing-method]',center).focus();return}}b.disabled=true;var r=await db.rpc('set_my_anchor_filing_state',{p_application_id:id,p_status:status,p_filed_on:null,p_method:method||null});b.disabled=false;if(r.error){q('[data-filing-status-copy]',center).textContent='Filing status could not be saved. Try again.';return}await refreshFiling(center)})});qa('[data-reminder]',center).forEach(function(box){box.addEventListener('change',async function(){var id=appId(),status=q('[data-reminder-status]',center);box.disabled=true;var r=await db.rpc('set_my_anchor_reminder',{p_application_id:id,p_offset_days:Number(box.dataset.reminder),p_enabled:box.checked});box.disabled=false;if(r.error){box.checked=!box.checked;status.textContent='Reminder could not be updated. Try again.'}else{status.textContent=box.checked?'Reminder scheduled from the verified State deadline.':'Reminder removed.'}await refreshFiling(center)})});await refreshFiling(center)}
function applyReuse(payload){SAFE_REUSE.forEach(function(name){var value=pathGet(payload,name),el=q('[name="'+name+'"]',form);if(el&&value!==undefined&&value!==null&&String(value).trim()&&!String(el.value||'').trim()){el.value=String(value);el.dispatchEvent(new Event('input',{bubbles:true}));el.dispatchEvent(new Event('change',{bubbles:true}))}})}
async function discoverReuse(){var box=q('[data-anchor-reuse]',root);if(!box||box.dataset.checked==='1')return;box.dataset.checked='1';try{var listed=await vault.listApplications(),apps=(listed.applications||[]).filter(function(r){return Number(r.tax_year)<TAX_YEAR&&r.status!=='archived'}).sort(function(a,b){return Number(b.tax_year)-Number(a.tax_year)});if(!apps.length)return;var key=await vault.getRememberedKey();if(!key)return;var payload=await vault.decryptApplication(apps[0],key);if(!payload)return;reuseCandidate=payload;box.hidden=false}catch(_){} }
function bindReuse(){var box=q('[data-anchor-reuse]',root);q('[data-anchor-reuse-yes]',root).addEventListener('click',function(){if(reuseCandidate)applyReuse(reuseCandidate);box.hidden=true;reuseCandidate=null;setSaveState('saving');markSaving()});q('[data-anchor-reuse-no]',root).addEventListener('click',function(){box.hidden=true;reuseCandidate=null})}
function processStep(){var step=activeStep();updateVisited();helpFor(step);if(step==='review')reviewAudit();if(step==='complete')installFilingCenter();if(step==='vault'||step==='profile')discoverReuse()}
function bind(){q('[data-anchor-save-exit]',root).addEventListener('click',saveAndExit);qa('[data-anchor-jump]',root).forEach(function(b){b.addEventListener('click',function(){if(!b.disabled)jumpBack(b.dataset.anchorJump)})});q('[data-anchor-help-open]',root).addEventListener('click',function(){openHelp(activeStep())});qa('[data-anchor-help-close]',root).forEach(function(b){b.addEventListener('click',closeHelp)});form.addEventListener('click',function(e){var why=e.target.closest('.wd-anchor-why');if(why){e.preventDefault();openHelp(why.dataset.helpStep);return}if(e.target.closest('[data-next]'))inlineValidate()},true);form.addEventListener('input',markSaving,true);form.addEventListener('change',markSaving,true);new MutationObserver(processStep).observe(form,{subtree:true,attributes:true,attributeFilter:['class']});bindReuse();installWhy();processStep();confirmSaved()}
async function mount(){if(document.getElementById('wd-anchor-process-ui'))return;var css=document.createElement('link');css.rel='stylesheet';css.href='/property/css/anchor-application-process.css?v=20260912a';css.dataset.anchorProcess='1';document.head.appendChild(css);try{var r=await fetch(PARTIAL,{credentials:'same-origin'});if(!r.ok)throw new Error('partial');var host=document.createElement('div');host.innerHTML=await r.text();root=host.firstElementChild;if(!root)return;var status=q('#wd-app-status');status.parentNode.insertBefore(root,status.nextSibling);bind()}catch(_){} }
mount();
})();