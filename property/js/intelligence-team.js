(function(){'use strict';const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>Array.from(r.querySelectorAll(s)),esc=v=>String(v==null?'':v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),rank=p=>({standard:0,agent:1,pro:2,pro_plus:3,teams:4,developer:5}[p]??0);let client=null,plan='standard',organizations=[],active=null,members=[],scopes=[],jobs=[];function toast(m){const t=$('#pl-toast');if(!t)return;t.textContent=m;t.style.display='block';clearTimeout(window.__wti);window.__wti=setTimeout(()=>t.style.display='none',3000)}function orgCard(o){return `<button type="button" class="wti-org ${active&&String(active.id)===String(o.id)?'on':''}" data-org="${esc(o.id)}"><span><b>${esc(o.name)}</b><small>${esc(o.role||'member')}</small></span><span class="wti-pill">${esc(o.role||'member')}</span></button>`}function memberCard(m){return `<article class="wti-member"><span><b>${esc(m.name||m.email||'Watchdog member')}</b><small>${esc(m.email||m.user_id)}</small></span><span class="wti-role">${esc(m.role)}</span></article>`}function scopeCard(s){return `<article class="wti-scope"><div><span><b>${esc(s.name)}</b><small>${esc(String(s.scope_type||'').replace(/_/g,' '))} · ${Number((s.resolved_pams_pins||[]).length).toLocaleString()} properties</small></span><span class="wti-pill">${s.is_scheduled?esc(s.cadence||'daily'):'saved'}</span></div><small>${(s.model_keys||[]).map(m=>String(m).replace(/_/g,' ')).join(' + ')}</small></article>`}function jobCard(j){return `<article class="wti-job"><span><b>${esc(String(j.model_key||'').replace(/_/g,' '))}</b><small>${Number(j.processed_count||0).toLocaleString()} / ${Number(j.candidate_count||0).toLocaleString()} · v${esc(j.model_version)}</small></span><span class="wti-role">${esc(String(j.status||'queued').replace(/_/g,' '))}</span></article>`}function render(){const host=$('#wti-app');if(rank(plan)<4){host.innerHTML='<div class="wti-gate"><i class="fas fa-lock"></i><h2>Teams Intelligence requires Teams</h2><p>Shared Intelligence uses explicit organization membership. Pro and Pro+ findings remain private to the individual account.</p><a class="wti-button" href="/property/pro#plans">Compare plans</a></div>';return}host.innerHTML=`<div class="wti-grid"><aside class="wti-stack"><section class="wti-card"><header><span>WORKSPACES</span><h2>Teams Intelligence</h2><p>Only explicit members can read shared scopes, runs and findings.</p></header><div class="wti-stack">${organizations.length?organizations.map(orgCard).join(''):'<div class="wti-empty"><p>No shared workspace yet.</p></div>'}</div><form id="wti-create" class="wti-form" style="margin-top:12px"><label>New workspace<input id="wti-org-name" maxlength="120" placeholder="South Jersey Team"></label><button type="submit">Create workspace</button></form></section>${active?`<section class="wti-card"><header><span>MEMBERS</span><h2>${esc(active.name)}</h2><p>Your role: ${esc(active.role)}</p></header><div class="wti-members">${members.length?members.map(memberCard).join(''):'<div class="wti-empty"><p>No members returned.</p></div>'}</div>${['owner','admin'].includes(active.role)?`<form id="wti-member" class="wti-form" style="margin-top:12px"><label>Exact Watchdog account email<input id="wti-member-email" type="email" required></label><label>Role<select id="wti-member-role"><option value="member">Member</option><option value="viewer">Viewer</option><option value="admin">Admin</option></select></label><button type="submit">Add or update member</button><div class="wti-note">This lookup uses the exact email only. It does not search similar people or expose an account directory.</div></form>`:''}</section>`:''}</aside><main class="wti-stack">${active?`<section class="wti-card"><header><span>SHARED POPULATION</span><h2>Schedule shared Intelligence</h2><p>Shared runs keep organization lineage and remain readable only to workspace members.</p></header><form id="wti-scope" class="wti-form"><label>Scope<select id="wti-type"><option value="municipality">Municipality</option><option value="county">County</option></select></label><label>Market<input id="wti-value" required maxlength="100" placeholder="Cherry Hill Twp"></label><label>Name<input id="wti-scope-name" maxlength="140" placeholder="Cherry Hill daily review"></label><label>Cadence<select id="wti-cadence"><option value="daily">Daily</option><option value="weekly">Weekly</option></select></label><label><span><input id="wti-change" type="checkbox" checked style="width:auto"> Include Change Intelligence</span></label><button type="submit" id="wti-submit">Save shared schedule and queue run</button><div id="wti-status" class="wti-status"></div></form></section><section class="wti-card"><header><span>SHARED SCOPES</span><h2>Monitored populations</h2></header><div class="wti-scopes">${scopes.length?scopes.map(scopeCard).join(''):'<div class="wti-empty"><p>No shared scopes yet.</p></div>'}</div></section><section class="wti-card"><header><span>SHARED JOBS</span><h2>Population processing</h2></header><div class="wti-jobs">${jobs.length?jobs.map(jobCard).join(''):'<div class="wti-empty"><p>No shared jobs yet.</p></div>'}</div></section>`:'<div class="wti-empty"><i class="fas fa-users"></i><h3>Create or select a Teams workspace</h3><p>Shared Intelligence is never inferred from brokerage, email domain, or CRM identity. Membership is explicit.</p></div>'}</main></div>`;wire()}
async function loadOrg(){members=[];scopes=[];jobs=[];if(!active){render();return}const [m,s,j]=await Promise.all([client.functions.invoke('intelligence-team',{body:{action:'members',organization_id:active.id}}),client.from('intelligence_scopes').select('*').eq('organization_id',active.id).eq('is_active',true).order('updated_at',{ascending:false}).limit(100),client.from('intelligence_jobs').select('id,organization_id,scope_id,model_key,model_version,status,candidate_count,processed_count,finding_count,created_at').eq('organization_id',active.id).order('created_at',{ascending:false}).limit(50)]);members=m.error?[]:m.data?.members||[];scopes=s.data||[];jobs=j.data||[];render()}
async function load(){const state=await client.functions.invoke('intelligence-team',{body:{action:'state'}});if(state.error)throw state.error;organizations=state.data?.organizations||[];if(active){active=organizations.find(o=>String(o.id)===String(active.id))||organizations[0]||null}else active=organizations[0]||null;await loadOrg()}
function wire(){$$('[data-org]').forEach(b=>b.onclick=async()=>{active=organizations.find(o=>String(o.id)===String(b.dataset.org))||null;await loadOrg()});$('#wti-create')?.addEventListener('submit',async e=>{e.preventDefault();const name=$('#wti-org-name').value.trim();if(!name)return;const r=await client.functions.invoke('intelligence-team',{body:{action:'create',name}});if(r.error){toast('Could not create the Teams workspace.');return}active={...r.data.organization,role:r.data.role};toast('Teams workspace created.');await load()});$('#wti-member')?.addEventListener('submit',async e=>{e.preventDefault();const r=await client.functions.invoke('intelligence-team',{body:{action:'add_member',organization_id:active.id,email:$('#wti-member-email').value.trim(),role:$('#wti-member-role').value}});if(r.error){toast('That exact Watchdog account could not be added.');return}toast('Teams membership updated.');await loadOrg()});$('#wti-scope')?.addEventListener('submit',async e=>{e.preventDefault();const button=$('#wti-submit'),status=$('#wti-status'),models=['assessment_anomaly'];if($('#wti-change').checked)models.push('property_change_priority');button.disabled=true;status.textContent='Resolving the governed shared population...';try{const r=await client.functions.invoke('intelligence-team-job-submit',{body:{organization_id:active.id,scope_type:$('#wti-type').value,scope_value:{value:$('#wti-value').value.trim()},name:$('#wti-scope-name').value.trim()||`${active.name} shared Intelligence`,model_keys:models,is_scheduled:true,cadence:$('#wti-cadence').value,run_now:true}});if(r.error)throw r.error;status.textContent=`Saved. ${Number(r.data?.resolved_count||0).toLocaleString()} shared properties resolved.`;await loadOrg()}catch(_){status.textContent='Watchdog could not save that shared scope.'}finally{button.disabled=false}})}
async function init(){try{await Promise.resolve(window.njptrAccessReady);client=window.NJPTRAccess?.client?.();if(!client)return;const usage=await client.rpc('get_agent_usage');plan=usage.data?.plan||'standard';if(rank(plan)>=4)await load();else render()}catch(_){const host=$('#wti-app');if(host)host.innerHTML='<div class="wti-empty"><p>Teams Intelligence could not load this workspace.</p></div>'}}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();})();
