(function(){'use strict';const $=(s,r=document)=>r.querySelector(s),esc=v=>String(v==null?'':v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let client=null,timer=null;function ms(v){const n=Number(v);if(!Number.isFinite(n)||n<0)return'Not measured';if(n<1000)return`${Math.round(n)} ms`;if(n<60000)return`${(n/1000).toFixed(1)} sec`;return`${(n/60000).toFixed(1)} min`}function percent(v){const n=Number(v);return Number.isFinite(n)?`${(n*100).toFixed(1)}%`:'0.0%'}function number(v,d=0){const n=Number(v);return Number.isFinite(n)?n.toLocaleString('en-US',{maximumFractionDigits:d}):'0'}function pill(value){const v=String(value||'unknown'),cls=/live|calibrated|complete/i.test(v)?'good':/failed|uncalibrated|preview|draft/i.test(v)?'warn':'';return `<span class="wio-pill ${cls}">${esc(v.replace(/_/g,' '))}</span>`}function modelRow(m){return `<article class="wio-model"><div><h3>${esc(m.label||m.model_key)}</h3><p>${esc(m.model_key)} · v${esc(m.version)}</p></div><span>${pill(m.status)}</span><span>${pill(m.calibration_state)}</span><span><b>${esc(m.minimum_plan||'')}</b>minimum plan</span><span><b>${esc(m.objective||'')}</b>objective</span></article>`}function jobRow(j){return `<tr><td>${esc(String(j.model_key||'').replace(/_/g,' '))} v${esc(j.model_version)}</td><td class="wio-status ${esc(j.status)}">${esc(String(j.status||'').replace(/_/g,' '))}</td><td>${number(j.processed_count)}/${number(j.candidate_count)}</td><td>${number(j.finding_count)}</td><td>${Number(j.retry_count||0)}</td><td>${j.started_at&&j.completed_at?ms(new Date(j.completed_at)-new Date(j.started_at)):'-'}</td></tr>`}function calibrationRows(data){const rows=data?.rows||[];if(!rows.length)return'<div class="wio-note">No calibration rollup table is available in this environment. Model state still comes from the immutable model/version registries.</div>';return `<div class="wio-calibration">${rows.slice(0,10).map(r=>`<article><b>${esc(r.model_key||r.name||'Calibration')}</b><small>${esc(r.calibration_state||r.status||'recorded')} · ${esc(r.created_at||r.updated_at||'')}</small></article>`).join('')}</div>`}function render(d){const host=$('#wio-app');if(!host)return;const j=d.jobs||{},f=d.findings||{},p=d.provider||{},s=d.schedules||{},c=d.cache||{};host.innerHTML=`<section class="wio-grid"><article class="wio-metric ${Number(j.failure_rate||0)>0.05?'bad':'good'}"><span>Job failure rate</span><b>${percent(j.failure_rate)}</b><small>${number(j.failed)} failed of ${number(j.total)}</small></article><article class="wio-metric"><span>Cache hit rate</span><b>${percent(j.cache_hit_rate)}</b><small>${number(c.total_hits)} historical cache hits</small></article><article class="wio-metric"><span>P95 job completion</span><b>${ms(j.p95_completion_ms)}</b><small>24-hour window</small></article><article class="wio-metric"><span>Evidence coverage</span><b>${number(f.average_evidence_coverage,1)}%</b><small>${number(f.total)} findings</small></article><article class="wio-metric"><span>Provider tokens</span><b>${number(Number(p.input_tokens||0)+Number(p.output_tokens||0))}</b><small>${number(p.requests)} Analyst requests</small></article><article class="wio-metric ${Number(j.stale_running||0)?'bad':'good'}"><span>Stale jobs</span><b>${number(j.stale_running)}</b><small>${number(s.due)} schedules currently due</small></article></section><div class="wio-layout"><div class="wio-stack"><section class="wio-card"><header><div><span>MODEL REGISTRY</span><h2>Current governed models</h2><p>Promotion state and calibration remain explicit and versioned.</p></div><button class="wio-refresh" id="wio-refresh"><i class="fas fa-rotate"></i> Refresh</button></header><div class="wio-models">${(d.models||[]).map(modelRow).join('')}</div></section><section class="wio-card"><header><div><span>POPULATION JOBS</span><h2>Recent worker activity</h2><p>Queued, resumable, cache-aware deterministic processing.</p></div></header><table class="wio-jobs"><thead><tr><th>Model</th><th>Status</th><th>Progress</th><th>Findings</th><th>Retries</th><th>Runtime</th></tr></thead><tbody>${(j.recent||[]).slice(0,30).map(jobRow).join('')||'<tr><td colspan="6">No population jobs in this window.</td></tr>'}</tbody></table></section></div><aside class="wio-stack"><section class="wio-card"><header><div><span>PIPELINE HEALTH</span><h2>24-hour operating view</h2></div></header><div class="wio-health"><div><span>Queued jobs</span><b>${number(j.queued)}</b></div><div><span>Running / partial</span><b>${number(Number(j.running||0)+Number(j.partial||0))}</b></div><div><span>Completed</span><b>${number(j.complete)}</b></div><div><span>Cache hits</span><b>${number(j.cache_hit)}</b></div><div><span>Active schedules</span><b>${number(s.active)}</b></div><div><span>Active cache entries</span><b>${number(c.active_entries)}</b></div><div><span>Average confidence</span><b>${number(f.average_confidence,1)}%</b></div><div><span>Average Watchdog score</span><b>${number(f.average_score,1)}</b></div></div></section><section class="wio-card"><header><div><span>AI PROVIDER</span><h2>Usage and fail-soft state</h2></div></header><div class="wio-health"><div><span>Input tokens</span><b>${number(p.input_tokens)}</b></div><div><span>Output tokens</span><b>${number(p.output_tokens)}</b></div><div><span>P95 request latency</span><b>${ms(p.p95_latency_ms)}</b></div><div><span>Recorded cost</span><b>${p.estimated_cost_cents==null?'Not configured':`$${(Number(p.estimated_cost_cents)/100).toFixed(2)}`}</b></div></div><div class="wio-note warn">${esc(p.note||'Watchdog does not invent provider cost when a versioned pricing record is unavailable.')}</div></section><section class="wio-card"><header><div><span>CALIBRATION</span><h2>Promotion evidence</h2></div></header>${calibrationRows(d.calibration)}</section></aside></div>`;$('#wio-refresh').onclick=load}
async function load(){const host=$('#wio-app');try{const r=await client.functions.invoke('intelligence-operations-admin',{body:{}});if(r.error)throw r.error;render(r.data)}catch(e){const status=Number(e?.context?.status||e?.status||0);host.innerHTML=`<div class="wio-gate"><i class="fas ${status===403?'fa-lock':'fa-triangle-exclamation'}"></i><h2>${status===403?'Developer access required':'Operations unavailable'}</h2><p>${status===403?'This control plane is not visible to customer accounts.':'The Intelligence operations summary could not be loaded.'}</p></div>`}}
async function init(){try{await Promise.resolve(window.njptrAccessReady);client=window.NJPTRAccess?.client?.();if(!client)return;await load();timer=setInterval(()=>{if(!document.hidden)load()},30000)}catch(_){}}window.addEventListener('beforeunload',()=>timer&&clearInterval(timer));if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();})();
