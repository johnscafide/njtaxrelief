(function(){'use strict';
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>Array.from(r.querySelectorAll(s));
const ICONS={long_term_ownership:'fa-house-circle-check',recent_purchase:'fa-key',older_housing_stock:'fa-screwdriver-wrench',high_tax_exposure:'fa-scale-balanced'};
const COLORS={long_term_ownership:'seller',recent_purchase:'welcome',older_housing_stock:'services',high_tax_exposure:'tax'};
const ALIASES={sicklerville:['GLOUCESTER TWP','WINSLOW TWP'],blackwood:['GLOUCESTER TWP'],turnersville:['WASHINGTON TWP'],sewell:['WASHINGTON TWP','MANTUA TWP'],mullica_hill:['HARRISON TWP'],west_berlin:['BERLIN TWP'],berlin:['BERLIN BORO','BERLIN TWP'],voorhees:['VOORHEES TWP'],marlton:['EVESHAM TWP'],cherry_hill:['CHERRY HILL TWP']};
let client=null,data=null;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));
function toast(m){const t=$('#ms-toast');if(!t)return;t.textContent=m;t.style.display='block';clearTimeout(window.__msDiscToast);window.__msDiscToast=setTimeout(()=>t.style.display='none',3800)}
async function rpc(name,args={}){const r=await client.rpc(name,args);if(r.error)throw r.error;return r.data}
function key(v){return String(v||'').trim().toLowerCase().replace(/[^a-z0-9]+/g,'_').replace(/^_|_$/g,'')}
function candidates(v){const raw=String(v||'').trim();if(!raw)return[null];const a=ALIASES[key(raw)]||[];return[raw.toUpperCase(),...a.filter(x=>x!==raw.toUpperCase())]}
function shell(){if($('#ms-discovery'))return $('#ms-discovery');const plays=$('#plays');if(!plays)return null;const section=document.createElement('section');section.className='ms-card ms-wide msd-shell';section.id='ms-discovery';section.innerHTML=`<div class="msd-head"><div><span class="ms-eyebrow">FIND AN AUDIENCE</span><h2>Who do you want to reach?</h2><p>Search by a New Jersey town, municipality or familiar place name. Watchdog will translate common town names to the municipality used in property records when possible.</p></div><div class="msd-search"><label>Town or municipality<input id="msd-town" placeholder="Try Sicklerville, Winslow Twp, Voorhees…" autocomplete="off"><small class="msd-town-help">You do not need the exact tax-record municipality.</small><div class="msd-town-suggestions"><button type="button" data-town="Sicklerville">Sicklerville</button><button type="button" data-town="Voorhees">Voorhees</button><button type="button" data-town="Sewell">Sewell</button></div></label><label>Max audience<select id="msd-limit"><option>250</option><option>500</option><option selected>1000</option><option>2500</option><option>5000</option></select></label><button class="ms-primary" id="msd-run"><i class="fas fa-magnifying-glass"></i> Find audience</button></div></div><div id="msd-results" class="msd-results"><div class="ms-empty">Enter a place above, or leave it blank to discover opportunities across available New Jersey property records.</div></div>`;plays.before(section);bind();return section}
function bind(){const run=$('#msd-run');if(run)run.onclick=discover;$$('[data-town]').forEach(b=>b.onclick=()=>{const f=$('#msd-town');f.value=b.dataset.town;discover()});const f=$('#msd-town');if(f)f.onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();discover()}}}
async function discover(){const btn=$('#msd-run'),input=$('#msd-town')?.value.trim()||'',tries=candidates(input);if(btn){btn.disabled=true;btn.innerHTML='<i class="fas fa-circle-notch fa-spin"></i> Searching'}try{let used=tries[0];for(const town of tries){data=await rpc('marketing_discover_opportunities',{p_town:town});const count=(data?.plays||[]).reduce((n,p)=>n+Number(p.property_count||0),0);used=town;if(count>0||town===tries[tries.length-1])break}render(used,input)}catch(e){toast(e.message||'Opportunity discovery could not run.')}finally{if(btn){btn.disabled=false;btn.innerHTML='<i class="fas fa-magnifying-glass"></i> Find audience'}}}
function render(used,input){const root=$('#msd-results');if(!root)return;const plays=data?.plays||[],count=plays.reduce((n,p)=>n+Number(p.property_count||0),0),translated=input&&used&&key(input)!==key(used);root.innerHTML=`${translated&&count?`<div class="msd-town-notice"><b>${esc(input)}</b> is represented in property records under <b>${esc(used)}</b>. We used that municipality automatically.</div>`:''}${plays.map(p=>`<article class="msd-card ${COLORS[p.play_key]||''}"><div class="msd-visual"><i class="fas ${ICONS[p.play_key]||'fa-bullseye'}"></i><div><span>WATCHDOG SIGNAL</span><b>${Number(p.property_count||0).toLocaleString()}</b><small>matching properties</small></div></div><div class="msd-copy"><div class="msd-tags"><span>${esc(p.profession)}</span><span>${esc(p.goal).replaceAll('_',' ')}</span></div><h3>${esc(p.title)}</h3><p>${esc(p.explanation)}</p><details><summary>Why these properties?</summary><p>${esc(p.explanation)} The segment is built from property-record fields available to Watchdog; it is not a claim about a person's intent or protected characteristics.</p></details><button class="ms-primary" data-create-play="${esc(p.play_key)}" ${Number(p.property_count||0)===0?'disabled':''}>Use this audience</button></div></article>`).join('')||'<div class="ms-empty">No opportunity segments were found. Try a nearby municipality or leave the town blank to search statewide.</div>'}`;$$('[data-create-play]').forEach(b=>b.onclick=()=>createCampaign(b.dataset.createPlay,b,used))}
async function createCampaign(play,btn,used){const town=used||$('#msd-town')?.value.trim()||null,limit=Number($('#msd-limit')?.value||1000);btn.disabled=true;const old=btn.textContent;btn.textContent='Creating…';try{const r=await rpc('marketing_create_campaign_from_opportunity',{p_play_key:play,p_town:town,p_limit:limit});toast(`${Number(r.property_count||0).toLocaleString()} properties added to a new campaign.`);location.href=`/property/marketing-studio.html?campaign=${encodeURIComponent(r.campaign_id)}#creative-studio`}catch(e){toast(e.message||'Could not create the campaign.');btn.disabled=false;btn.textContent=old}}
async function start(){try{await window.njptrAccessReady;client=window.NJPTRAccess.client();let tries=0;const wait=()=>{const s=shell();if(s)return;tries+=1;if(tries<60)setTimeout(wait,100)};wait()}catch(_){}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();
})();