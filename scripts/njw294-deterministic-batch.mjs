import fs from 'node:fs';

function mustReplace(path,before,after,label){
  let s=fs.readFileSync(path,'utf8');
  const count=s.split(before).length-1;
  if(count!==1) throw new Error(`${label}: expected one target, got ${count}`);
  s=s.replace(before,after);
  fs.writeFileSync(path,s);
}

const derived='supabase/functions/workbench-derived/index.ts';
let d=fs.readFileSync(derived,'utf8');
if(!d.includes("const ENGINE_VERSION = 'watchdog-derived-v22-njw291';")) throw new Error('engine version target missing');
d=d.replace("const ENGINE_VERSION = 'watchdog-derived-v22-njw291';","const ENGINE_VERSION = 'watchdog-derived-v23-njw294';");
const chapterAnchor="      else if (def.operation === 'chapter123_field')";
if((d.split(chapterAnchor).length-1)!==1) throw new Error('chapter anchor missing or duplicated');
const ops=`      else if (def.operation === 'parcel_record_volatility') { const changes=num(value(cfg.change_dep)),history=yearObject(value(cfg.history_dep)); if(changes!=null&&changes>=0&&history){ const years=Object.keys(history).filter((y)=>/^(19|20)\\d{2}$/.test(y)).map(Number).filter(Number.isFinite).sort((a,b)=>a-b); let compared=0; for(let i=1;i<years.length;i++) if(years[i]===years[i-1]+1) compared++; if(compared>0&&changes<=compared){ v=round(clamp(changes/compared*100),1); auxMeta.set(id,{changed_consecutive_transitions:changes,compared_consecutive_transitions:compared,history_years:years,volatility_contract:'percent of actual consecutive annual MOD-IV transitions with any retained record-field change',missing_years_synthesized:false}); } } }\n      else if (def.operation === 'source_release_freshness') { const gate=value(cfg.gate_dep),release=Date.parse(String(cfg.release_date||'')),cadence=num(cfg.cadence_days); if(present(gate)&&Number.isFinite(release)&&cadence!=null&&cadence>0){ const nowDate=new Date(),today=Date.UTC(nowDate.getUTCFullYear(),nowDate.getUTCMonth(),nowDate.getUTCDate()),rd=new Date(release),releaseDay=Date.UTC(rd.getUTCFullYear(),rd.getUTCMonth(),rd.getUTCDate()),ageDays=Math.max(0,Math.floor((today-releaseDay)/86400000)); v=round(clamp(100-ageDays/cadence*100),1); auxMeta.set(id,{source_release_date:String(cfg.release_date),source_age_days:ageDays,declared_refresh_cadence_days:cadence,freshness_contract:'100 on governed source release date; declines linearly to 0 at the declared refresh-cadence horizon',gate_dependency:String(cfg.gate_dep||'')}); } }\n`;
d=d.replace(chapterAnchor,ops+chapterAnchor);
fs.writeFileSync(derived,d);

const formulasPath='property/data/derived-marker-formulas.json';
const formulas=JSON.parse(fs.readFileSync(formulasPath,'utf8'));
formulas.markers['watchdog.njplus.parcel_record_volatility']={
  range:'0-100; percent of actual consecutive annual MOD-IV transitions with any retained record-field change',
  formula:'parcel_record_change_count / actual consecutive observed property-class transitions * 100; gaps are skipped and no years are synthesized',
  professional_reason:'Shows how frequently the parcel public record changed across comparable annual MOD-IV transitions; it is not a property-condition finding.'
};
formulas.markers['watchdog.njplus.housing_program_record_freshness']={
  range:'0-100 source-record freshness; 100 on the governed workbook release date and 0 at/after the declared annual refresh horizon',
  formula:'100 - elapsed whole days since the 2026-07-01 governed DCA Affordable Housing workbook release / 365 * 100, clamped 0-100; municipality reporting-status evidence is required',
  professional_reason:'Shows source-record currency against the DCA source family’s declared annual refresh cadence; it does not assert that an underlying housing fact has or has not changed.'
};
formulas.markers['watchdog.njplus.pilot_revenue_concentration']={
  range:'percent of DCA 3-year average property valuation; legacy marker ID retained for compatibility',
  formula:'NJ DCA aggregate PILOT assessed value / max(NJ DCA UFB 3-year average property valuation, 1) * 100',
  professional_reason:'Legacy-ID assessed-value exposure proxy. It compares observed PILOT assessed value with published municipal valuation and is not PILOT revenue, a revenue share, or a revenue forecast.'
};
fs.writeFileSync(formulasPath,JSON.stringify(formulas,null,2)+'\n');

fs.writeFileSync('supabase/migrations/20260829141000_prepare_njw294_deterministic_batch.sql',`-- NJW-294: admit two deterministic operations and stage three formulas for production canary.\nalter table public.derived_formula_registry drop constraint if exists derived_formula_registry_operation_check;\nalter table public.derived_formula_registry add constraint derived_formula_registry_operation_check check (operation is null or operation = any (array['year_delta','ratio','completeness','inverse','permit_closure','permit_activity','weighted_signals','signal_count','signal_density','weighted_scores','max_scores','product_scores','tax_rate_position','municipal_cost_absorption','fiscal_resilience','revaluation_pressure','transaction_tax_shock','investor_carry_volatility','tax_reset_sensitivity','marketability_drag','sr1a_subject_square_feet','comparable_evidence_reliability','assessment_defensibility','appeal_evidence_strength','appeal_opportunity','source_alias','chapter123_field','ordered_history','history_metric','chapter123_position','assessment_component_shift','parcel_record_volatility','source_release_freshness']::text[]));\n\ninsert into public.derived_formula_registry(marker_id,engine_version,formula,dependencies,confidence,status,explanation,operation,config,updated_at) values\n('watchdog.njplus.parcel_record_volatility','watchdog-derived-v23-njw294','parcel_record_change_count / actual consecutive observed property-class transitions * 100; gaps skipped',array['njplus.nj-dca-modiv-longitudinal.parcel_record_change_count','njplus.nj-dca-modiv-longitudinal.property_class_history']::text[],'high','live','Percent of actual consecutive annual MOD-IV transitions with any retained record-field change. Missing years are skipped; no years are synthesized; not a property-condition finding.','parcel_record_volatility',jsonb_build_object('change_dep','njplus.nj-dca-modiv-longitudinal.parcel_record_change_count','history_dep','njplus.nj-dca-modiv-longitudinal.property_class_history'),now()),\n('watchdog.njplus.housing_program_record_freshness','watchdog-derived-v23-njw294','100 - elapsed whole days since governed DCA workbook release / declared annual cadence * 100, clamped 0-100',array['njplus.nj-dca-affordable-housing.affordable_housing_reporting_status']::text[],'high','live','Source-record freshness against the governed NJ DCA Affordable Housing workbook release and declared annual cadence. Reporting-status evidence is required. This is provider/source currency, not a claim that an underlying housing fact has or has not changed.','source_release_freshness',jsonb_build_object('gate_dep','njplus.nj-dca-affordable-housing.affordable_housing_reporting_status','release_date','2026-07-01','cadence_days',365),now()),\n('watchdog.njplus.pilot_revenue_concentration','watchdog-derived-v23-njw294','aggregate DCA PILOT assessed value / max(DCA UFB 3-year average property valuation,1) * 100',array['njplus.nj-dca-pilot-forecast.pilot_project_assessment','njplus.nj-dca-ufb-2025.net_debt_three_year_avg_property_valuation']::text[],'high','live','Legacy marker ID retained for compatibility. The calculated value is an assessed-value concentration proxy using two observed DCA facts; it is not PILOT revenue, revenue share, or a forecast.','ratio',jsonb_build_object('num','njplus.nj-dca-pilot-forecast.pilot_project_assessment','den','njplus.nj-dca-ufb-2025.net_debt_three_year_avg_property_valuation','den_min',1,'scale',100,'precision',4),now())\non conflict(marker_id) do update set engine_version=excluded.engine_version,formula=excluded.formula,dependencies=excluded.dependencies,confidence=excluded.confidence,status=excluded.status,explanation=excluded.explanation,operation=excluded.operation,config=excluded.config,updated_at=now();\n\ncreate or replace function public.dispatch_provider_release_canary(p_scenario text)\nreturns bigint language plpgsql set search_path to 'public','extensions' as $$\ndeclare v_raw text := encode(gen_random_bytes(48), 'hex'); v_email text := 'watchdog-provider-canary-' || substr(v_raw,1,10) || '@example.com'; v_request_id bigint;\nbegin\n if p_scenario not in ('zoning_v31','designation_stack_v15','csrr_v1','csrr_controls_v2','csrr_semantics_v3','geology_intersections_v1','model_bounds_v1','uniformity_history_v1','uniformity_history_v2','uniformity_history_v3','modiv_longitudinal_v1','modiv_longitudinal_partial_scan_v1','modiv_longitudinal_missing_year_v1','community_assets_access_v1','zoning_contact_status_v1','pilot_observed_v1','v036_sources_v1','modiv_record_change_v1','affordable_housing_v037','development_trends_v038','ufb_v039','ufb_longitudinal_v040','njw294_deterministic_v1') then raise exception 'Unsupported release canary scenario'; end if;\n insert into public.watchdog_test_bootstrap_tokens(token_hash,desired_email,redirect_to,expires_at,metadata) values (encode(digest(v_raw,'sha256'),'hex'),v_email,'https://njpropertytaxrelief.com/property/dashboard',now()+interval '10 minutes',jsonb_build_object('purpose','provider_release_canary','scenario',p_scenario,'no_real_spend',true));\n select net.http_post(url := 'https://uvkvaxljhhngydvlrzom.supabase.co/functions/v1/provider-release-canary',headers := jsonb_build_object('Content-Type','application/json'),body := jsonb_build_object('token',v_raw,'scenario',p_scenario),timeout_milliseconds := 30000) into v_request_id; return v_request_id;\nend; $$;\n`);

fs.writeFileSync('supabase/functions/provider-release-canary/njw294-deterministic-batch-canary.ts',`import { createClient } from 'npm:@supabase/supabase-js@2.95.0';\nconst URL=Deno.env.get('SUPABASE_URL')!,ANON=Deno.env.get('SUPABASE_ANON_KEY')!,SERVICE=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!;\nconst admin=createClient(URL,SERVICE,{auth:{persistSession:false,autoRefreshToken:false}});\nconst SCENARIO='njw294_deterministic_v1',PARCEL='watchdog.njplus.parcel_record_volatility',HOUSING='watchdog.njplus.housing_program_record_freshness',PILOT='watchdog.njplus.pilot_revenue_concentration';\nconst CHANGE='njplus.nj-dca-modiv-longitudinal.parcel_record_change_count',CLASS='njplus.nj-dca-modiv-longitudinal.property_class_history',HSTATUS='njplus.nj-dca-affordable-housing.affordable_housing_reporting_status',PASS='njplus.nj-dca-pilot-forecast.pilot_project_assessment',VALUATION='njplus.nj-dca-ufb-2025.net_debt_three_year_avg_property_valuation';\nconst ABSECON='0101_25.01_10',ATLANTIC_CITY='0102_139_15';\nfunction json(status:number,p:any){return new Response(JSON.stringify(p),{status,headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store, private'}})}\nasync function hash(v:string){const d=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(v));return Array.from(new Uint8Array(d)).map(b=>b.toString(16).padStart(2,'0')).join('')}\nasync function cleanup(id:string){await admin.from('watchdog_test_accounts').delete().eq('user_id',id);await admin.from('account_entitlements').delete().eq('user_id',id);await admin.from('profiles').delete().eq('id',id);await admin.auth.admin.deleteUser(id)}\nfunction n(v:any){if(v===null||v===undefined||v==='')return null;const x=Number(v);return Number.isFinite(x)?x:null}\nfunction round(v:number,p=1){const f=10**p;return Math.round(v*f)/f}\nfunction consecutive(history:any){if(!history||typeof history!=='object'||Array.isArray(history))return 0;const years=Object.keys(history).filter(y=>/^(19|20)\\d{2}$/.test(y)).map(Number).filter(Number.isFinite).sort((a,b)=>a-b);let c=0;for(let i=1;i<years.length;i++)if(years[i]===years[i-1]+1)c++;return c}\nfunction freshness(){const now=new Date(),today=Date.UTC(now.getUTCFullYear(),now.getUTCMonth(),now.getUTCDate()),release=Date.UTC(2026,6,1),age=Math.max(0,Math.floor((today-release)/86400000));return{age,score:round(Math.max(0,Math.min(100,100-age/365*100)),1)}}\nasync function post(path:string,body:any,access:string){const r=await fetch(URL+'/functions/v1/'+path,{method:'POST',headers:{Authorization:'Bearer '+access,apikey:ANON,'Content-Type':'application/json'},body:JSON.stringify(body)});let p:any={};try{p=await r.json()}catch{}return{ok:r.ok,status:r.status,p}}\nexport async function handleNjw294DeterministicBatchCanary(req:Request){let body:any={};try{body=await req.json()}catch{return json(400,{error:'Invalid JSON'})}const token=String(body?.token||'').trim();if(String(body?.scenario||'')!==SCENARIO||!/^[A-Za-z0-9_-]{40,160}$/.test(token))return json(401,{error:'Invalid release canary request'});const now=new Date().toISOString();const {data:gate}=await admin.from('watchdog_test_bootstrap_tokens').update({used_at:now}).eq('token_hash',await hash(token)).is('used_at',null).gt('expires_at',now).contains('metadata',{purpose:'provider_release_canary',scenario:SCENARIO}).select('id,desired_email').maybeSingle();if(!gate)return json(401,{error:'Invalid or expired release canary token'});let userId='';try{const {data:link,error:linkError}=await admin.auth.admin.generateLink({type:'magiclink',email:String(gate.desired_email||'')});userId=String(link?.user?.id||'');const hashed=String(link?.properties?.hashed_token||'');if(linkError||!userId||!hashed)throw new Error('sandbox_link_generation_failed');const authClient=createClient(URL,ANON,{auth:{persistSession:false,autoRefreshToken:false,detectSessionInUrl:false}});const {data:v,error:ve}=await authClient.auth.verifyOtp({token_hash:hashed,type:'email'});const access=v?.session?.access_token||'';if(ve||!access)throw new Error('sandbox_session_verification_failed');const pr=await admin.from('profiles').upsert({id:userId,email:String(gate.desired_email||''),full_name:'Watchdog NJW-294 Canary',display_name:'Watchdog NJW-294 Canary',account_role:'developer',plan_tier:'standard',plan:'free',profile_complete:true,custom:{watchdog_test_account:true,no_real_spend:true,release_canary:true,scenario:SCENARIO}},{onConflict:'id'});if(pr.error)throw new Error('sandbox_profile_failed');const ta=await admin.from('watchdog_test_accounts').upsert({user_id:userId,label:'NJW-294 Deterministic Canary',last_bootstrap_at:now,metadata:{no_real_spend:true,scenario:SCENARIO}},{onConflict:'user_id'});if(ta.error)throw new Error('sandbox_account_failed');const started=Date.now(),pins=[ABSECON,ATLANTIC_CITY],rawIds=[CHANGE,CLASS,HSTATUS,PASS,VALUATION];const hydrate=await post('workbench-hydrate',{pams_pins:pins,marker_ids:rawIds},access);const derived=await post('workbench-derived',{pams_pins:pins,marker_ids:[PARCEL,HOUSING,PILOT]},access);const mismatches:string[]=[];const observations:any={};\nconst ch=n(hydrate.p?.markers?.[ABSECON]?.[CHANGE]),hist=hydrate.p?.markers?.[ABSECON]?.[CLASS],pairs=consecutive(hist),parcelExpected=ch!=null&&pairs>0&&ch<=pairs?round(ch/pairs*100,1):null,parcelActual=derived.p?.markers?.[ABSECON]?.[PARCEL],parcelMeta=derived.p?.meta?.[ABSECON]?.[PARCEL]||{};observations.parcel={expected:parcelExpected,actual:parcelActual,changes:ch,compared:pairs,kind:parcelMeta.provider_kind||null};if(parcelExpected==null||Number(parcelActual)!==parcelExpected||parcelMeta.provider_kind!=='derived_governed')mismatches.push('parcel_record_volatility');\nconst hstat=hydrate.p?.markers?.[ABSECON]?.[HSTATUS],fresh=freshness(),housingActual=derived.p?.markers?.[ABSECON]?.[HOUSING],housingMeta=derived.p?.meta?.[ABSECON]?.[HOUSING]||{};observations.housing={reporting_status:hstat,age_days:fresh.age,expected:fresh.score,actual:housingActual,kind:housingMeta.provider_kind||null};if(hstat===null||hstat===undefined||Number(housingActual)!==fresh.score||housingMeta.provider_kind!=='derived_governed')mismatches.push('housing_program_record_freshness');\nconst pa=n(hydrate.p?.markers?.[ATLANTIC_CITY]?.[PASS]),val=n(hydrate.p?.markers?.[ATLANTIC_CITY]?.[VALUATION]),pilotExpected=pa!=null&&val!=null&&val>0?round(pa/Math.max(val,1)*100,4):null,pilotActual=derived.p?.markers?.[ATLANTIC_CITY]?.[PILOT],pilotMeta=derived.p?.meta?.[ATLANTIC_CITY]?.[PILOT]||{};observations.pilot={pilot_assessed_value:pa,three_year_avg_property_valuation:val,expected:pilotExpected,actual:pilotActual,kind:pilotMeta.provider_kind||null,semantic_contract:'assessed-value concentration proxy; not revenue'};if(pilotExpected==null||Number(pilotActual)!==pilotExpected||pilotMeta.provider_kind!=='derived_governed')mismatches.push('pilot_revenue_concentration');\nconst ok=hydrate.ok&&derived.ok&&mismatches.length===0;await admin.from('watchdog_test_auth_events').insert({token_id:gate.id,user_id:userId,event_type:'provider_release_canary',metadata:{scenario:SCENARIO,status_code:ok?200:502,duration_ms:Date.now()-started,assertion_ok:ok,mismatches,observations}});return json(ok?200:502,{ok,scenario:SCENARIO,assertion_ok:ok,mismatches,observations,duration_ms:Date.now()-started,engine_version:derived.p?.engine_version||null});}catch(e){return json(500,{ok:false,scenario:SCENARIO,error:String((e as Error)?.message||e)})}finally{if(userId)await cleanup(userId)}}\n`);

fs.writeFileSync('supabase/functions/provider-release-canary/production-v043-bootstrap.ts',`import { handleNjw294DeterministicBatchCanary } from './njw294-deterministic-batch-canary.ts';\nconst nativeServe=Deno.serve.bind(Deno);\nconst wrappedServe=((first:unknown,second?:unknown)=>{const wrap=(handler:Deno.ServeHandler):Deno.ServeHandler=>async(req,info)=>{let scenario='';try{scenario=String((await req.clone().json())?.scenario||'')}catch{}if(scenario==='njw294_deterministic_v1')return handleNjw294DeterministicBatchCanary(req);return handler(req,info);};if(typeof first==='function')return nativeServe(wrap(first as Deno.ServeHandler));if(typeof second==='function')return nativeServe(first as Deno.ServeOptions,wrap(second as Deno.ServeHandler));return nativeServe(first as Deno.ServeOptions);}) as typeof Deno.serve;\nObject.defineProperty(Deno,'serve',{configurable:true,writable:true,value:wrappedServe});\nawait import('./production-v042-bootstrap.ts');\n`);

fs.writeFileSync('docs/property/njw-294-deterministic-batch-certification-2026-08-29.md',`# NJW-294 deterministic marker batch\n\nCandidate markers remain fail-closed until authenticated production canary certification and provider-coverage promotion.\n\n- Parcel Record Volatility: percent of actual consecutive annual MOD-IV transitions with any retained record-field change; gaps are skipped.\n- Housing Program Record Freshness: source-record currency from the governed 2026-07-01 DCA workbook release over its declared annual cadence.\n- PILOT Revenue Concentration legacy ID: assessed-value concentration proxy using DCA aggregate PILOT assessed value divided by DCA UFB 3-year average property valuation. It is explicitly not PILOT revenue or a forecast.\n`);

console.log('NJW-294 deterministic source batch prepared');
